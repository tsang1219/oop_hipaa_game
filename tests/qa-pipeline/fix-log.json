[
  {
    "iteration": 1,
    "bug_id": "BUG-001",
    "agent": "fix-agent",
    "commit": "00b6992",
    "files_changed": ["tests/helpers/qa-helpers.ts"],
    "result": "applied",
    "verification": "code matches documented fix_approach; full playwright re-run deferred to iteration 2 orchestrator",
    "notes": "Added dismissSorterIfPresent() helper. Called from loadRoom, loadFresh, goThroughDoor, interactWith. Test infra only — no production code touched."
  },
  {
    "bug_id": "BUG-005",
    "agent": "fix-agent",
    "fix_plan": "Replace `font-sans` className with inline `style={{ fontFamily: '\"Press Start 2P\", monospace' }}` on the two body-text <p> tags (WelcomeStep line 85, TowerPlacedStep line 163) in OnboardingOverlay.tsx. Drop text-sm/text-xs to text-[9px] so the pixel font remains readable inside the existing card sizes. Typewriter effect untouched — it operates on React state (displayedText) and is independent of font styling. Not modifying the SelectTowerStep / PlaceTowerStep components or the MISSION BRIEF / TOWER PLACED! header lines because those already use Press Start 2P correctly.",
    "result": "success",
    "files_changed": ["client/src/components/breach-defense/OnboardingOverlay.tsx"],
    "diff_summary": "Replaced `font-sans text-sm/text-xs` with `text-[9px]` + inline Press Start 2P font on the two body <p> tags. Typewriter logic and step structure unchanged.",
    "lines_added": 4,
    "lines_removed": 2,
    "net_delta": 2,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "POLISH_STANDARD Section 6 Rule 6.1 satisfied: all visible UI text now renders in Press Start 2P. Two body paragraphs were the only remaining font-sans offenders in this file."
  },
  {
    "bug_id": "BUG-002",
    "agent": "fix-agent",
    "fix_plan": "The bug ticket states that 'fixed inset-0' (full viewport) must be replaced with 'absolute inset-0' (canvas-relative) in three places: SorterContextCard.tsx:33, PHISorterOverlay.tsx:46 (error fallback), and PHISorterOverlay.tsx:228 (main overlay). The wrong-feedback toast at PHISorterOverlay.tsx:316 also uses 'fixed bottom-6' which anchors to viewport — convert to 'absolute bottom-6' so it anchors to the canvas-relative overlay parent. Both files mount inside the 'relative' canvas container in UnifiedGamePage.tsx (line 1236), so 'absolute' resolves to canvas bounds (960x720). Minimum changes: 4 className edits across 2 files; not changing any logic, props, or structure.",
    "result": "success",
    "files_changed": [
      "client/src/components/phi-sorter/SorterContextCard.tsx",
      "client/src/components/phi-sorter/PHISorterOverlay.tsx"
    ],
    "diff_summary": "Replaced 'fixed' with 'absolute' positioning in 4 places (1 in SorterContextCard, 3 in PHISorterOverlay) so PHI Sorter overlays render canvas-relative within the 960x720 game container instead of viewport-relative.",
    "lines_added": 4,
    "lines_removed": 4,
    "net_delta": 0,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "POLISH_STANDARD Section 1 Rule 1.1: modals must be canvas-relative. Mount context confirmed: UnifiedGamePage.tsx line 1236 wraps PhaserGame + overlays in <div className='relative ...'>, so 'absolute inset-0' anchors to that 960x720 container. All 4 'fixed' positioning classes converted; toast (line 316) kept bottom-6/left-1/2 anchoring but is now relative to canvas overlay parent rather than viewport."
  },
  {
    "bug_id": "BUG-003",
    "agent": "fix-agent",
    "fix_plan": "In ExplorationScene.create() NPC render loop (around line 930), after constructing nameLabel with setOrigin(0.5, 0), measure nameLabel.width and clamp the label's x position so its left/right edges stay within [labelPad, roomPxWidth - labelPad] using Phaser.Math.Clamp. roomPxWidth = room.width * TILE. This auto-detects edge cases for any NPC near a room boundary regardless of name length, without modifying roomData.json or per-NPC content. Origin/tween/completed-alpha behavior preserved; only the x coordinate is adjusted post-construction.",
    "result": "success",
    "files_changed": ["client/src/phaser/scenes/ExplorationScene.ts"],
    "diff_summary": "Added 9-line clamp block after nameLabel construction in NPC render loop: measures nameLabel.width and clamps x to keep label inside roomPxWidth - padding on both edges.",
    "lines_added": 10,
    "lines_removed": 0,
    "net_delta": 10,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Clamp is symmetric (handles both left and right overflow). Guard `if (maxX >= minX)` skips the clamp if the label is wider than the room itself (degenerate case) so we don't pin it off-canvas. Boss text and checkmark labels above NPC head are short enough not to overflow and were left untouched per minimum-fix rule."
  },
  {
    "bug_id": "BUG-004a",
    "agent": "fix-agent",
    "fix_plan": "Audit of BattleEncounterScreen, ChoicePrompt, and EducationalItemModal showed ChoicePrompt was already fully wired (open + select). Two real silent player actions remained: (1) BattleEncounterScreen's initial mount (phase='dialogue' open) had no SFX and the dialogue-advance path (SPACE keypress, overlay click, dialogue panel click, CONTINUE button on feedback) was silent; close on unmount was also silent. (2) EducationalItemModal's overlay-click close path bypassed the existing close-button SFX. Fix wires sfx_interact at subtle volumes (0.35 open, 0.3 dialogue advance, 0.4 CONTINUE, 0.2 rate=0.9 close) using the existing eventBridge.REACT_PLAY_SFX pattern already imported in both files. No new SFX keys, no new files, no new abstractions; volumes match the per-action guidance in the ticket and existing house style elsewhere in the codebase.",
    "result": "success",
    "files_changed": [
      "client/src/components/BattleEncounterScreen.tsx",
      "client/src/components/EducationalItemModal.tsx"
    ],
    "diff_summary": "BattleEncounterScreen: added sfx_interact on mount (0.35), on unmount (0.2 rate=0.9), on SPACE-to-advance (0.3), on overlay click advance (0.3), on dialogue panel click advance (0.3), and on CONTINUE button (0.4). EducationalItemModal: wrapped overlay-click close to play sfx_interact (0.2 rate=0.9) before onClose. ChoicePrompt left untouched — already complete.",
    "lines_added": 15,
    "lines_removed": 2,
    "net_delta": 13,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "POLISH_STANDARD Section 4 Rule 4.1 / Commandment 1: every player-initiated action now produces audio feedback. Used only existing SFX (sfx_interact) per BUG-004a constraint; new SFX sourcing deferred to BUG-004b. Volumes kept subtle (0.2-0.4 range) so micro-feedbacks don't compete with music or fanfare moments. ChoicePrompt and the existing dialogue-choice select pathway were already wired in prior commits and were intentionally left untouched."
  },
  {
    "bug_id": "BUG-006",
    "agent": "fix-agent",
    "fix_plan": "Extend dismissSorterIfPresent() to also dismiss the PHISorterOverlay that auto-opens after the SorterContextCard is confirmed. After the existing card-confirm step, poll for [data-testid=\"phi-sorter-overlay\"] (full poll only when we just dismissed a card; otherwise a cheap visibility check) and click [data-testid=\"button-sorter-close\"] (falls back to keyboard Escape). Wait for overlay detach + scene unpause. Also added dismissSorterIfPresent calls at the END of talkToNPC/examineZone/collectItem so the post-dialogue re-trigger (abort path resets encounterTriggered=false) can be cleared without leaving the sorter UI to block subsequent interactions.",
    "result": "needs_review",
    "files_changed": ["tests/helpers/qa-helpers.ts"],
    "diff_summary": "1) dismissSorterIfPresent: keeps the existing context-card branch (now non-early-return), then polls for phi-sorter-overlay, dismisses via close button (or Escape fallback), waits for detach. Smart cost: only pays the 1s overlay poll when we just dismissed a card. 2) talkToNPC, examineZone, collectItem: added dismissSorterIfPresent call after dialogue closes / pause clears, to handle re-triggers caused by abort path resetting encounterTriggered.",
    "lines_added": 28,
    "lines_removed": 5,
    "net_delta": 23,
    "verification_test": "npx tsc --noEmit && npx playwright test tests/progression/door-unlocks.spec.ts --workers=1",
    "verification_result": "tsc clean. door-unlocks: 6/8 passing, 2 still failing (Break room unlocks after completing reception, Records room unlocks after completing lab). HOWEVER: pre-fix error-context showed the FULL PHISorterOverlay UI ('NOT PHI', 'Safe to Share', drag items) blocking — that is now GONE. Post-fix error-context shows only the standard HUD with NPCs 1/3 — meaning the second NPC interaction (nervous_patient at 5,8 / researcher at 3,5) silently fails for an unrelated reason after the first NPC succeeds. Both failing rooms have the sorter trigger; the residual failure is plausibly an interaction race between the abort path (which resets encounterTriggered=false and unpauses) and the next interactWith teleport+space, but the overlay itself no longer blocks interactions.",
    "notes": "BUG-006 fix as specified is APPLIED and confirmed working: the PHI Sorter overlay no longer blocks subsequent interactions. The remaining 2 failures are a residual issue beyond the ticket scope — recommend follow-up bug for the encounterTriggered=false re-trigger race condition. Test infra only; no production code touched. DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx, nor revert the !isDemoActive() gate."
  },
  {
    "bug_id": "DESIGN-001",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "room": "er",
    "fix_plan": "Implement all 3 of the orchestrator's improvement plan items inside ExplorationScene's room render path, gated by `if (this.room.id === 'er')` block placed after the NPC loop. (1) Wall-mounted EMERGENCY status panel: dark backing rectangle + red glow rectangle + 'EMERGENCY' Press Start 2P label, with a yoyo alpha tween (0.6s, Sine.easeInOut, repeat -1) on the glow + label so it pulses red. Positioned tile-relative at (10, 1) just below the top wall, so it doesn't overlap the whiteboard at (10, 2). (2) Walking nurse: non-interactive Phaser sprite using the existing npc_nurse texture (via npcTextureKey('nurse_chen')), patrolling x=8.5 → x=11.5 at y=4.5 (above the nurse_station at obstacles 8,5,4,3). 1.8s yoyo tween with onYoyo/onRepeat flipX so the nurse faces the direction of travel. Adds a small drop shadow + a fast scaleY oscillation to read as walking footstep bob. (3) Officer + frantic_family fidget: locates the existing NPC interactables by id and adds a subtle angle wobble (-1.5° to +1.5°, 1.1s yoyo, slight per-NPC duration jitter) on top of the existing breathing tween from the standard NPC pipeline. Uses only existing textures + existing tween infra. No new files. No NPC dialogue/scenarios/IDs/zones changed. No SFX added.",
    "result": "success",
    "files_changed": ["client/src/phaser/scenes/ExplorationScene.ts"],
    "changes_implemented": ["flashing_emergency_panel", "walking_nurse_npc", "officer_and_family_idle_fidget"],
    "lines_added": 29,
    "lines_removed": 0,
    "net_delta": 29,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Phase B design improvement (not a bug). All 3 orchestrator items shipped within budget (1 file, +29 net lines, well under max 3 files / 50 lines). Targets ER's flat-affect problem: room now has a pulsing red urgency cue (panel), a sense-of-life moving NPC (nurse), and differentiated idle states on the two pressure-source NPCs (officer + family). Commandment 1 (every action needs a response) and Commandment 7 (peaks need texture) both addressed. DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx. DID NOT revert the PHI sorter !isDemoActive() gate. DID NOT add new SFX assets. DID NOT touch existing NPC dialogue/scenarios/IDs/zones (only added new decorative tweens + a non-interactable patrolling sprite + 3 graphics primitives)."
  },
  {
    "bug_id": "DESIGN-003",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "room": "break_room",
    "fix_plan": "Implement all 3 orchestrator items inside ExplorationScene's room render path, gated by `if (this.room.id === 'break_room')` block placed after the NPC pulse logic (just before the Player section). (1) Vending machine LEDs: iterate room.obstacles, for each vending_machine obstacle place a 2x2 pixel rectangle at the upper-right of the unit and color-cycle it every 700ms through 4 retro-arcade hues (red/yellow/green/blue) using a Phaser time event. (2) Microwave beep cycle: locate the single microwave obstacle, every 9 seconds play sfx_interact at volume 0.12 + rate 1.6 (subtle high-pitched ding) and spawn a transient 24x24 cream-yellow flash rectangle that fades out over 350ms. Volume well under the 0.15 cap. (3) Gossip ellipsis bubble: locate the gossiping_coworker NPC by id from this.interactables, every 4.5 seconds spawn a Press Start 2P text bubble (8px '...', '!', or '?' randomly) above-right of the sprite with dark rounded background, fade-in/hold/fade-out via yoyo tween. Uses only existing sfx_interact key + existing tween/time infra. No new files, no roomData changes, no NPC dialogue/scenarios/IDs/zones changes.",
    "result": "success",
    "files_changed": ["client/src/phaser/scenes/ExplorationScene.ts"],
    "changes_implemented": ["vending_machine_led_blink", "microwave_beep_cycle", "gossip_ellipsis_bubble"],
    "lines_added": 30,
    "lines_removed": 0,
    "net_delta": 30,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Phase B design improvement (not a bug). All 3 orchestrator items shipped within budget (1 file, +30 net lines, under max 3 files / 50 lines). Targets Break Room's lowest dimension (moment_of_joy: 6) by adding three layers of comedic life: pixel LEDs cycling on the vending machines (texture), a microwave that periodically dings (inhabited feel), and the Gossiping Coworker visibly gossiping with no actual dialogue (overheard-conversation flair). Commandment 4 (NPCs are people, not rule-delivery systems — gossip bubbles humanize without exposition) and Commandment 10 (surprise creates memories — the LED color shift, the unexpected microwave ding, the bubble glyph that catches your eye). Microwave volume capped at 0.12 (under the 0.15 max). DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx. DID NOT revert the PHI sorter !isDemoActive() gate. DID NOT add new SFX assets — sfx_interact only. DID NOT touch existing NPC dialogue/scenarios/IDs/zones (only added decorative time events + transient graphics + a transient text bubble; no interaction targets, no completion gates, no zone changes)."
  },
  {
    "bug_id": "DESIGN-005",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "room": "reception",
    "fix_plan": "Implement all 3 orchestrator items inside ExplorationScene's room render path, gated by `if (this.room.id === 'reception')` placed after the ER block (and before the existing NPC pulse logic). (1) Busy desk props: render three small decorative sprites on the desk surface row (y=2) — a tilted clipboard (obj_manual @ scale 0.55, angle -8), a coffee mug (furn_coffee_mug @ scale 0.6) with a 1px steam-bob yoyo tween, and a papers stack (furn_inbox_tray @ scale 0.55). All non-collision Phaser sprites, depth 5, riding 6px above the desk top so they read as tabletop items. (2) Vary chair groupings: add three decorative chair sprites that break the uniform waiting-area look — a 'conversation pair' at (10.0, 11) and (11.5, 11) angled inward toward each other (one flipX), plus an angled 'sat-askew' chair at (14, 8) leaning -25°. Pure decoration (no collision, no roomData edit). (3) Interactable pulse: iterate this.interactables and apply a subtle scale-yoyo (1.0 → 1.04, 1.5s, Sine.easeInOut, repeat -1) to every zone + item sprite in the room, signposting which objects the player can interact with. Uses only existing textures (obj_manual, furn_coffee_mug, furn_inbox_tray, furn_chair) + existing tween infra. No new files, no roomData changes, no NPC dialogue/scenarios/IDs/zones changes.",
    "result": "success",
    "files_changed": ["client/src/phaser/scenes/ExplorationScene.ts"],
    "changes_implemented": ["busy_desk_props", "varied_chair_groupings", "interactable_pulse_tween"],
    "lines_added": 25,
    "lines_removed": 0,
    "net_delta": 25,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Phase B design improvement (not a bug). All 3 orchestrator items shipped within budget (1 file, +25 net lines, half the 50-line cap). Targets Reception's moment_of_joy: 6 — the FIRST room a demo player enters and where they form a snap judgment. Desk props give the room an 'inhabited' feel (someone works here, they have coffee, paperwork is in motion). Chair variety breaks the grid-aligned uniform look that reads 'spreadsheet' rather than 'place where humans sit'. The interactable pulse addresses Commandment 1 (every action needs a response — and signposts where actions ARE possible) plus Commandment 9 (show progress — the pulse invites attention to incomplete zones/items). DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx. DID NOT revert the PHI sorter !isDemoActive() gate. DID NOT add new SFX assets. DID NOT touch existing NPC dialogue/scenarios/IDs/zones — all six new sprites are non-interactable decoration; the pulse tween is layered on top of the existing zone/item sprites without altering interaction logic, completion gates, or roomData."
  },
  {
    "bug_id": "DESIGN-004",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "room": "hospital_entrance",
    "fix_plan": "Three concrete changes per ticket — (1) coffee cart / info kiosk prop near entrance, (2) Riley + plant idle micro-animations, (3) ambient lobby chatter audio at very low volume (0.05-0.1) using existing sfx_interact. All gated by `if (this.room.id === 'hospital_entrance')` after the existing room-block chain in ExplorationScene.create().",
    "result": "needs_review",
    "files_changed": [],
    "changes_implemented": ["coffee_cart_prop_with_steam_puff", "riley_angle_sway_idle", "plant_leaf_sway_overlay", "ambient_chatter_blip"],
    "lines_added": 0,
    "lines_removed": 0,
    "net_delta": 0,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "commit": "f72fb21 (already shipped — bundled with the DESIGN-005 commit)",
    "notes": "DESIGN-004 work is ALREADY PRESENT in HEAD. The full `if (this.room.id === 'hospital_entrance')` block lives at ExplorationScene.ts lines 1109-1138 and contains all three ticketed changes plus a steam-puff bonus: (a) coffee_station sprite at tile (5,5) with drop shadow + periodic steam puff every 2.6s; (b) Riley angle wobble (-1.2°/+1.2°, 1700ms yoyo) on top of the standard breathing tween, and translucent green leaf-sway oval (-6°/+6°, ~1.9-2.3s yoyo with per-plant jitter) on each plant obstacle; (c) ambient lobby chatter via sfx_interact at volume 0.06 (well under the 0.1 cap) every 10.5s with rate 0.55-0.75 for variety. All constraints satisfied: no new SFX assets, no roomData edits, no NPC dialogue/scenarios/IDs/zones touched, did not modify StartMenu/TitleScreen/index.css/PixelComputerLogo, did not revert the !isDemoActive() PHI sorter gate. Block was added in commit f72fb21 (titled DESIGN-005 Reception) but the previous fix-agent run only logged DESIGN-005 in fix-log.json — leaving DESIGN-004 unrecorded despite being shipped. tsc clean against current HEAD with the existing block in place. No new commit created: the work is done, all 3 orchestrator items satisfied, and a duplicate commit would be churn. Recommend: design-review re-rates hospital_entrance against the existing implementation to confirm the >=8.0 lift."
  },
  {
    "bug_id": "DESIGN-006",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "room": "records_room",
    "fix_plan": "Implement 4 layered improvements inside ExplorationScene's room render path, gated by `if (this.room.id === 'records_room')` after the break_room block. (1) Flickering fluorescent tube + dust motes: warm-yellow rectangle 'tube' + soft halo above the upper filing aisle (tile x=10, y=1) with a periodic flicker (alpha drop to 0.25 + recovery, every 3.2s) and dust motes spawning every 1.4s drifting downward in the light beam (4.2s fade, randomized x within the beam). (2) Idle fidget on the two strongest NPCs — Chief Compliance Officer and Attorney: subtle angle wobble (-1.2°/+1.2°, 1300ms+jitter yoyo) layered on top of the standard breathing tween, matching the ER officer/family pattern. (3) Subpoena envelope prop: a 0.5-scale obj_manual sprite tilted 12° next to the Attorney NPC, with a slow 1px hover yoyo so it reads as an official document held out. Pure visual signal. (4) Decorative document cart drifting slowly: separate non-collision cart sprite (furn_document_cart) at scale 0.85 patrolling x=13 -> x=16 at y=9 (right of clerk_desk, clear of all obstacles) over 5.2s yoyo. Like the ER walking nurse pattern. Uses only existing textures (obj_manual, furn_document_cart) + existing tween/time infra. No new SFX (visual-only — paper rustle deferred to avoid asset addition). No roomData changes. No NPC dialogue/scenarios/IDs/zones touched.",
    "result": "success",
    "files_changed": ["client/src/phaser/scenes/ExplorationScene.ts"],
    "changes_implemented": ["fluorescent_tube_flicker_and_dust_motes", "cco_and_attorney_idle_fidget", "subpoena_envelope_prop", "decorative_document_cart_patrol"],
    "lines_added": 34,
    "lines_removed": 0,
    "net_delta": 34,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Phase B design improvement (not a bug). Records Room is the FINAL demo body room before the capstone — last impression before the certificate. 4 changes shipped within budget (1 file, +34 net lines, under the 50-line cap). Targets all three weak dimensions (moment_of_joy 7, polish 7, aesthetics 7): the flickering tube + dust motes give the archive a quiet, lived-in 'old records' atmosphere (aesthetics + polish); CCO + Attorney fidget makes the two highest-stakes NPCs feel alive while they wait for the player's decision (moment_of_joy + polish); the subpoena envelope is a tiny but immediately-readable visual signal that this NPC is here on official business (polish — Commandment 8: feedback scales with moment size); the drifting cart adds horizontal life across the otherwise-static lower aisle (moment_of_joy + Commandment 7 pacing texture). DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx. DID NOT revert the PHI sorter !isDemoActive() gate. DID NOT add new SFX assets (chose to skip the audio direction — visual-only kept the change tight and avoided new asset sourcing). DID NOT touch existing NPC dialogue/scenarios/IDs/zones — all additions are decorative tweens + transient graphics + non-interactable sprites; the existing static document_cart obstacle at (6,5) was left untouched and a SEPARATE decorative cart sprite was added in the open lower aisle so collision/completion logic is unaffected."
  },
  {
    "bug_id": "DESIGN-010",
    "agent": "fix-agent",
    "phase": "B-design-improvement",
    "screen": "capstone_certificate_overlay",
    "fix_plan": "Capstone is the LAST sponsor moment (current 7.8 avg, gameplay/gameplay_mechanic both 7). Picked 3 polish items inside CertificateOverlay.tsx — no new SFX, no index.css edits, all keyframes inline. (1) Dialogue advance feedback: added local advanceTick state bumped on each advance(); applied as React `key` on the speech-box div + new `cert-dialogue-pulse` keyframe (1.0 → 1.025 → 1.0, 220ms ease-out) so the bubble re-mounts and re-runs the pulse on every SPACE/ENTER/click. The existing sfx_interact (vol 0.4) chime stays. Crunchier, tactile confirmation that input landed. (2) NPC sprite breathing: added `cert-npc-bob` keyframe (translateY 0 → -2px → 0, 1.6s ease-in-out infinite) on the inline-style sprite div. Applied directly to the inner sprite div so it doesn't conflict with the parent column's one-shot translateX 'cert-npc-in' entry tween. Reads as 'NPC is alive while speaking' — Commandment 4. (3) Copy button bounce: added `cert-copy-bounce` keyframe (scale 1 → 1.06 → 1, 260ms ease-out) and applied conditionally via inline style when copied===true. Layers on top of the existing color-change + sfx_sorter_correct chime + COPIED ✓ label, giving the meaningful sponsor-code copy a proportional moment of feedback (Commandment 8).",
    "result": "success",
    "files_changed": ["client/src/components/CertificateOverlay.tsx"],
    "changes_implemented": ["dialogue_box_scale_pulse_on_advance", "npc_sprite_breathing_bob", "copy_button_scale_bounce"],
    "lines_added": 31,
    "lines_removed": 1,
    "net_delta": 30,
    "verification_test": "npx tsc --noEmit",
    "verification_result": "clean — no type errors",
    "notes": "Phase B design improvement (capstone polish — the LAST sponsor demo beat). Within budget: 1 file (cap 2), +30 net lines (cap 50). All 3 keyframes defined inline in the component's existing <style> block — index.css NOT touched (per DO-NOT-TOUCH list). No new SFX assets; reused sfx_interact (vol 0.4 ≤ cap) for advance and existing sfx_sorter_correct (vol 0.6) for copy. DID NOT modify StartMenu.tsx, TitleScreen.tsx, index.css, PixelComputerLogo.tsx. DID NOT revert the !isDemoActive() PHI sorter gate. Targeted the moderate dimensions (gameplay 7, gameplay_mechanic 7) by giving every player input a crunchier physical response (dialogue pulse, copy bounce) and giving the static moment more life (NPC bob)."
  }
]
