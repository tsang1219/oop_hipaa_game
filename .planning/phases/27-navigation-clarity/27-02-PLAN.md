---
phase: 27-navigation-clarity
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - client/src/phaser/scenes/ExplorationScene.ts
  - client/src/components/BattleEncounterScreen.tsx
autonomous: true
requirements: [VIS-08]

must_haves:
  truths:
    - "A feedback audit table in the SUMMARY lists every exploration-mode player action with its audio and visual response, and marks which were fixed this plan"
    - "Completing a zone updates the room immediately: its blue glow ring stops pulsing and fades out, and a green checkmark pops in — without leaving and re-entering the room"
    - "Completing an NPC scenario shows the same in-world checkmark live that a room reload shows, and the sprite dims with a fade instead of an instant snap"
    - "A wrong dialogue answer no longer plays the breach-alert horn at 0.45 — correct/incorrect use the purpose-fit sorter chime/soft-thud at proportional volume"
    - "tsc and production build are clean; no new modals exist"
  artifacts:
    - path: "client/src/phaser/scenes/ExplorationScene.ts"
      provides: "Live zone/NPC completion visuals in updateCompletionState (glow registry, checkmark pop-in, fade)"
      contains: "zoneGlows"
    - path: "client/src/components/BattleEncounterScreen.tsx"
      provides: "Proportional answer-feedback SFX (sfx_sorter_correct / sfx_sorter_wrong)"
      contains: "sfx_sorter_correct"
    - path: ".planning/phases/27-navigation-clarity/27-02-SUMMARY.md"
      provides: "Feedback audit table (Phase 27 success criterion 3)"
      contains: "Feedback Audit"
  key_links:
    - from: "UnifiedGamePage.tsx useEffect (~line 1167)"
      to: "ExplorationScene.updateCompletionState"
      via: "completedNPCs/completedZones/collectedItems sync on gameState change"
      pattern: "updateCompletionState"
    - from: "ExplorationScene zone render loop (~line 1053)"
      to: "updateCompletionState live-completion branch"
      via: "zoneGlows map populated at render, consumed on completion"
      pattern: "zoneGlows"
---

<objective>
Audit every player action in exploration mode against Commandment 1 (every action needs a response) and Commandment 8 (feedback scales with moment size), fix the gaps found in planning — live zone/NPC completion visuals and disproportionate wrong-answer audio — and gate the phase with tsc + build.

Purpose: Phase 27 criterion 3 — the moment-to-moment reward layer has no silent or jarring interactions.
Output: Glow-registry + live checkmark code in ExplorationScene, SFX swap in BattleEncounterScreen, audit table in SUMMARY.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/phaser/scenes/ExplorationScene.ts
@client/src/components/BattleEncounterScreen.tsx
@client/src/pages/UnifiedGamePage.tsx (read-only — completion sync useEffect ~1167, zone/item handlers ~732-764)
</context>

<interfaces>
<!-- Verified against the codebase 2026-06-10. -->

From client/src/phaser/scenes/ExplorationScene.ts:
```typescript
// Zone render loop (~1040-1081): incomplete zones get a pulsing blue stroke ring (zoneGlow, ~1054)
//   — the ring and its tween are currently NOT stored anywhere, so they cannot be stopped on live completion.
// Completed-at-render checkmarks: zones ~1071 (green ✓ above sprite), NPCs ~1151-1153 (✓ at y-20).
updateCompletionState(npcs: string[], zones: string[], items: string[])  // ~3135
//   currently: NPC → setAlpha(0.4) + setTint(0x888888) snap; item → setAlpha(0.4) + killTweensOf; zones → NOTHING.
// Called from UnifiedGamePage useEffect on every completedNPCs/completedZones/collectedItems change (~1167).
```

From client/src/components/BattleEncounterScreen.tsx (~74-81):
```typescript
if (feedback.type === 'correct') {
  eventBridge.emit(BRIDGE_EVENTS.REACT_PLAY_SFX, { key: 'sfx_tower_place', volume: 0.55 });
} else {
  eventBridge.emit(BRIDGE_EVENTS.REACT_PLAY_SFX, { key: 'sfx_breach_alert', volume: 0.45 });  // ← the honk
}
eventBridge.emit(BRIDGE_EVENTS.REACT_ANSWER_FEEDBACK, { type: feedback.type });
```

BootScene preloaded SFX keys (verified): sfx_footstep, sfx_interact, sfx_tower_place, sfx_enemy_death, sfx_breach_alert, sfx_wave_start, sfx_fanfare, sfx_sorter_correct (confirmation chime), sfx_sorter_wrong (soft error thud), sfx_sorter_stamp, sfx_sorter_paper. Use ONLY these — no new audio assets.

Audit findings from planning (seed for the audit table — executor verifies each line against current code):

| # | Action | Audio today | Visual today | Verdict |
|---|--------|-------------|--------------|---------|
| 1 | Walk (keyboard) | sfx_footstep 0.25, 350ms throttle (~2011) | dust puff particles (~2016) | OK |
| 2 | Click-to-move step | sfx_footstep 0.25 (~2795) | walk anim + dust | OK |
| 3 | NPC talk start | sfx_interact 0.55 (~2888) | dim overlay + first-time flash/sparkle (~2942) | OK |
| 4 | Zone examine start | sfx_interact 0.55 (~2888) | dim overlay | OK |
| 5 | Item collect | sfx_interact 0.4 r1.3 (UGP ~762) | gold flash + 8 particles (~3001) | OK |
| 6 | Hallway board read | sfx_interact 0.55 | light flash (~2990) | OK |
| 7 | Answer correct | sfx_tower_place 0.55 (BES ~74) | green overlay + particles + zoom pulse (~3026) | FIX: swap to sfx_sorter_correct (purpose-fit chime) |
| 8 | Answer incorrect | sfx_breach_alert 0.45 (BES ~76) | red overlay + gentle shake (~3061) | FIX: honk — swap to sfx_sorter_wrong, lower volume (Phase 20 FIX-03 precedent) |
| 9 | NPC completion tick | sfx_interact 0.5 + banner (UGP ~1206) | tint/alpha SNAP, no live checkmark (~3142) | FIX: fade + checkmark pop (parity with reload render ~1151) |
| 10 | Zone completion tick | none dedicated (dialogue feedback covers) | NOTHING until room reload — glow keeps pulsing | FIX: kill glow, checkmark pop, soft tick |
| 11 | Room complete | sfx_fanfare 0.9 (~2394) | gold flash + 30-particle burst | OK |
| 12 | Door enter | sfx_footstep 0.35 r0.8 (~2568) | 300ms camera fade | OK |
| 13 | Locked door bump | sfx_breach_alert 0.25 (~2704) | red flash + fadeIn | OK (Phase 20 already proportioned) |
| 14 | Encounter enter (TD) | sfx_breach_alert 0.35 (~2437) | camera shake + narrative card | OK |
| 15 | Encounter return | none | 400ms fadeIn + music restore (~2510) | OK — intentional release beat (document as such) |
| 16 | Idle hint sparkle (new, 27-01) | none (by design) | 3-particle shimmer | OK by design |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Live completion visuals — zone glow registry + checkmark pop-in + NPC fade parity</name>
  <files>client/src/phaser/scenes/ExplorationScene.ts</files>
  <action>
**1. Zone glow registry.** Add field `private zoneGlows: Map<string, { ring: Phaser.GameObjects.Arc; tween: Phaser.Tweens.Tween }> = new Map();` — clear it in `init()` (scene restart re-renders everything). In the zone render loop (~1053), store the created `zoneGlow` arc and its tween in the map keyed by `zone.id`.

**2. Extract a checkmark helper** so live completion and at-render completion produce identical marks:
```typescript
private addCompletionCheck(x: number, y: number, depth: number, pop = false): void
```
Renders the existing green '✓' text style (Press Start 2P, 7px, #44ff44, black stroke 2). When `pop` is true, start at `setScale(0)` and tween `scale: 1, duration: 250, ease: 'Back.easeOut'` (Zelda item-get pop — Commandment 6). Replace the two inline checkmark blocks (zones ~1071, NPCs ~1151) with calls (pop=false at render).

**3. Rewrite `updateCompletionState` (~3135)** to detect NEW completions by diffing against the previous sets BEFORE reassigning them:
- **Newly completed zone:** look up `zoneGlows.get(id)` — stop the tween, tween the ring `strokeAlpha: 0, scale: 0.6, duration: 300` then destroy, remove from map. Call `addCompletionCheck(sprite.x, sprite.y - 16, sprite.depth + 1, true)`. Play a soft tick: `try { this.sound.play('sfx_sorter_correct', { volume: 0.25, rate: 1.1 }); } catch (_) {}` — quiet completion tick, distinct from the louder NPC banner moment (Commandment 8: zone < NPC < room).
- **Newly completed NPC:** replace the alpha/tint snap with `this.tweens.add({ targets: ia.sprite, alpha: 0.4, duration: 400, ease: 'Sine.easeOut' })` + `setTint(0x888888)` (tint can stay immediate — it's subtle under the fade), and call `addCompletionCheck(ia.sprite.x, ia.sprite.y - 20, ia.sprite.depth + 1, true)`. Guard against double-adding (only fire for ids newly present in the incoming array vs the previous Set).
- **Already-completed entries** (present in both old and new sets): keep today's idempotent behavior (sprite stays dimmed) without re-popping checkmarks.
- Items unchanged (already handled, has flash + particles + SFX at interact time).

Note `this.scene.isActive()` guard at top — `updateCompletionState` is called from React and may race a transition; bail if the scene isn't active (the next room load renders completed state anyway).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && grep -q "zoneGlows" client/src/phaser/scenes/ExplorationScene.ts && grep -q "addCompletionCheck" client/src/phaser/scenes/ExplorationScene.ts</automated>
  </verify>
  <done>Completing a zone mid-session kills its glow ring and pops a green checkmark with a soft tick; completing an NPC fades the sprite and pops the same checkmark a reload would show; render-time and live-time marks are pixel-identical (shared helper); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Proportional answer SFX + audit table + regression gate</name>
  <files>client/src/components/BattleEncounterScreen.tsx</files>
  <action>
**1. SFX swap in BattleEncounterScreen (~74-78):**
- correct: `{ key: 'sfx_sorter_correct', volume: 0.45 }` (purpose-fit confirmation chime, replaces repurposed tower_place)
- incorrect: `{ key: 'sfx_sorter_wrong', volume: 0.35 }` (soft thud, replaces the breach-alert horn — same Commandment 8 rationale as Phase 20 FIX-03; the red overlay + shake from REACT_ANSWER_FEEDBACK carries the weight, audio is an accent)
Add a one-line comment citing Phase 27 VIS-08. Leave every other SFX emission in the file untouched (tower placement at ~74 area is TD-context and correct as-is — verify you are editing the dialogue feedback branch that also emits REACT_ANSWER_FEEDBACK at ~81).

**2. Verify the audit.** Walk the 16-row audit table in this plan's `<interfaces>` block against the CURRENT code (27-01 merged first — line numbers may have drifted; match on patterns, not line numbers). Confirm each "OK" row's audio + visual still exist, confirm rows 7-10 are now fixed (7-8 by this task, 9-10 by Task 1), and append any action discovered during the walk that the table missed (e.g., new idle-hint and next-door visuals from 27-01 — document both as designed-silent visuals). Correct any row whose current values differ.

**3. Regression gate:**
- `npm run check` and `npm run build` both clean.
- No new modals: confirm this phase added zero new full-screen/modal components — `git diff --stat` for the phase touches only ExplorationScene.ts, UnifiedGamePage.tsx, EventBridge.ts, BattleEncounterScreen.tsx, REQUIREMENTS.md + planning docs.
- Completion logic untouched: `git diff client/src/hooks/useGameState.ts` is empty.

**4. Write the final audit table into 27-02-SUMMARY.md** under a `## Feedback Audit` heading (Phase 27 success criterion 3 lives in the SUMMARY by design) with columns: Action | Audio | Visual | Status (OK / FIXED in 27-02 / by-design-silent).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && npm run build && grep -q "sfx_sorter_correct" client/src/components/BattleEncounterScreen.tsx && ! grep -q "sfx_breach_alert', volume: 0.45" client/src/components/BattleEncounterScreen.tsx</automated>
  </verify>
  <done>Wrong answers thud softly instead of honking; correct answers chime; audit table with all exploration actions and their dual-channel feedback recorded in SUMMARY; tsc + build clean; useGameState.ts untouched.</done>
</task>

</tasks>

<verification>
- `npm run check` and `npm run build` exit 0.
- 27-02-SUMMARY.md contains a `## Feedback Audit` table covering at minimum: move, NPC talk, zone examine, item collect, answer correct/incorrect, NPC/zone/room completion, door enter, locked bump, encounter enter/return.
- Manual spot-check (deferred to user): complete the sign-in sheet zone in Reception → blue ring fades, checkmark pops with a tick, no room reload needed; answer a dialogue question wrong → soft thud, no horn.
</verification>

<success_criteria>
- Phase 27 criterion 3: feedback audit table exists with every action showing audio + visual; previously-silent interactions (live zone/NPC completion) and disproportionate audio (wrong-answer honk) fixed.
- Phase 27 criterion 4: no new modals; tsc and production build clean.
- All Phase 12/15/20 door, fanfare, and FIX-03 behaviors unregressed.
</success_criteria>

<output>
After completion, create `.planning/phases/27-navigation-clarity/27-02-SUMMARY.md` containing the `## Feedback Audit` table.
</output>
