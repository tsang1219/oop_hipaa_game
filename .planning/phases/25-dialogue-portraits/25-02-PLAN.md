---
phase: 25-dialogue-portraits
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - client/src/components/DialoguePortrait.tsx
  - client/src/components/BattleEncounterScreen.tsx
  - client/src/components/NPCSprite.tsx
  - client/src/phaser/SpriteFactory.ts
autonomous: true
requirements: [VIS-01, VIS-03]
must_haves:
  truths:
    - "During any NPC dialogue, a >=96px pixelated portrait of the speaking NPC renders, cropped from the PNG sheet BootScene preloads"
    - "The portrait sits in a framed plate with the NPC's name and visibly breathes (2-3px bob, ~2.4s loop) while dialogue is open"
    - "The NPCSprite SVG placeholder renders nowhere; NPCSprite.tsx is deleted from the repo"
    - "Dialogue advance, choices, feedback, and scoring behave exactly as before — GameContainer.tsx has zero diff"
    - "npm run check and npm run build both pass"
  artifacts:
    - path: "client/src/components/DialoguePortrait.tsx"
      provides: "Framed 96px CSS-crop portrait with name plate and breathing-bob idle animation"
      min_lines: 40
    - path: "client/src/components/BattleEncounterScreen.tsx"
      provides: "Dialogue header rendering DialoguePortrait instead of NPCSprite"
      contains: "DialoguePortrait"
  key_links:
    - from: "client/src/components/BattleEncounterScreen.tsx"
      to: "client/src/components/DialoguePortrait.tsx"
      via: "npcId/npcName props"
      pattern: "<DialoguePortrait"
    - from: "client/src/components/DialoguePortrait.tsx"
      to: "client/src/data/spriteAssetPaths.ts"
      via: "getNPCPortraitPath(npcId)"
      pattern: "getNPCPortraitPath"
---

<objective>
Replace the 32px NPCSprite SVG placeholder in the dialogue overlay with a DialoguePortrait component: a >=96px pixelated CSS crop of the speaking NPC's real spritesheet, framed in a plate with the NPC's name and a subtle breathing-bob idle animation. Then delete NPCSprite.tsx.

Purpose: Phase 25 success criteria 1, 3, 4 — dialogue NPCs feel like people (Commandment 4), the placeholder SVG is gone everywhere, and dialogue logic is untouched.
Output: New DialoguePortrait.tsx, rewired BattleEncounterScreen header, NPCSprite.tsx deleted, clean tsc + production build.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/components/BattleEncounterScreen.tsx
@client/src/data/spriteAssetPaths.ts
@client/src/components/phi-sorter/NPCReactionBubble.tsx  (Phase 24 64px crop precedent — portrait block ~lines 177-201)

Constraints (verified during planning):
- NPCSprite.tsx has exactly ONE consumer: BattleEncounterScreen.tsx line 4 import + line 195 render. SpriteFactory.ts references it only in a comment (line 32). No test code imports it. After the swap it has zero consumers — delete it (per phase guidance).
- GameContainer passes `npcId={npcId || currentScene.character.toLowerCase().replace(/\s+/g, '_')}` — the fallback-derived id may not exist in roomData; getNPCPortraitPath handles that with a dev warning + staff sheet. Do NOT modify GameContainer.tsx (success criterion 4: zero regression).
- Keep `data-testid="npc-battle-sprite"` on the portrait container (QA bridge/visual tests reference the dialogue header).
- Sheet geometry: 96x128 PNG, 3x4 grid of 32x32 frames, frame 0 = idle-down at top-left. 96px render = 3x scale: `backgroundSize: '288px 384px'`, `backgroundPosition: '0 0'`, `imageRendering: 'pixelated'`.
</context>

<interfaces>
From client/src/data/spriteAssetPaths.ts (created by Plan 25-01):
```typescript
/** Resolves a roomData npcId to its PNG sheet path; staff-sheet fallback + dev console.warn for unknown ids. */
export function getNPCPortraitPath(npcId: string): string;
```

Current BattleEncounterScreen header block being replaced (lines 187-203):
```tsx
{/* Header: NPC portrait + name + optional privacy meter */}
<div className="flex items-center gap-3 px-4 pt-4 pb-2">
  <div
    className="flex-shrink-0 w-12 h-12 bg-[#16213e] border-2 border-[#FF6B9D] rounded flex items-center justify-center overflow-hidden"
    data-testid="npc-battle-sprite"
  >
    <div className="w-10 h-10">
      <NPCSprite npcId={npcId} direction="down" />
    </div>
  </div>
  <div className="flex-1 min-w-0">
    <span className="font-['Press_Start_2P'] text-[#FF6B9D] text-sm">{npcName}</span>
  </div>
  {/* ... TRUST meter (privacyScore) — keep as-is ... */}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create DialoguePortrait component</name>
  <files>client/src/components/DialoguePortrait.tsx</files>
  <action>
Create `DialoguePortrait` (default export) with props `{ npcId: string; npcName: string }`:

1. Resolve sheet path once: `const spriteUrl = getNPCPortraitPath(npcId);` from '@/data/spriteAssetPaths' (useMemo on npcId is fine but not required — it's a pure lookup).
2. Structure — a framed plate (Zelda-portrait presence, not an icon):
   - Outer plate: `border-4 border-[#FF6B9D] bg-[#16213e]` with a subtle inner shadow or 2px inner highlight, `data-testid="npc-battle-sprite"` (preserve existing testid), flex column, width fits 96px crop + padding.
   - Portrait crop layer (inner div): width/height 96px, `backgroundImage: url(spriteUrl)`, `backgroundSize: '288px 384px'`, `backgroundPosition: '0 0'`, `backgroundRepeat: 'no-repeat'`, `imageRendering: 'pixelated'`, `backgroundColor: '#1a1a2e'`. This matches the NPCReactionBubble 64px pattern scaled to 96px (3x).
   - Name plate strip under the crop, inside the frame: `font-['Press_Start_2P']` ~8px text, `text-[#FFD93D]`, dark strip background (`bg-[#1a1a2e]`), centered, `wordBreak: 'break-word'` so "Chief Compliance Officer" wraps instead of overflowing.
3. Idle life (VIS-03): inject a `<style>` tag (same inline pattern BattleEncounterScreen already uses for feedback-flash) defining
   `@keyframes portrait-breathe { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-2px); } }`
   and apply `animation: 'portrait-breathe 2.4s ease-in-out infinite'` to the CROP layer only — the frame and name plate stay rock-still so the bob reads as breathing, not jitter. 2px amplitude, never more — it must not distract from reading (Commandment 7 breathing room, not noise).
4. No sounds, no click handlers, no state — purely presentational. Component must render correctly for unknown npcIds too (staff sheet via resolver fallback).
  </action>
  <verify>
    <automated>npm run check && grep -q "portrait-breathe" client/src/components/DialoguePortrait.tsx && grep -q "getNPCPortraitPath" client/src/components/DialoguePortrait.tsx && grep -q "pixelated" client/src/components/DialoguePortrait.tsx</automated>
  </verify>
  <done>DialoguePortrait.tsx exists: 96px pixelated frame-0 crop, framed plate, in-frame name plate, breathing-bob keyframe on the crop layer only; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap portrait into BattleEncounterScreen</name>
  <files>client/src/components/BattleEncounterScreen.tsx</files>
  <action>
1. Replace `import NPCSprite from './NPCSprite';` with `import DialoguePortrait from './DialoguePortrait';`.
2. Replace the header's 48px portrait box (the `w-12 h-12 ... data-testid="npc-battle-sprite"` div and its NPCSprite child) with `<DialoguePortrait npcId={npcId} npcName={npcName} />`.
3. Layout adjustments — the portrait grows from 48px to ~110px tall, so:
   - Change header row from `items-center` to `items-start` (or `items-end`) so the TRUST meter doesn't float awkwardly mid-portrait; keep `flex gap-3 px-4 pt-4 pb-2`.
   - The name now lives inside the portrait plate — remove the standalone `<span>{npcName}</span>` block (or keep the flex-1 spacer div empty for layout). Do not render the name twice.
   - Keep the TRUST meter block byte-identical in behavior (same thresholds, width, transition).
4. Touch NOTHING else: typewriter effect, Space/click advance, choice buttons, feedback card, SFX emits, all data-testids (`battle-encounter-screen`, `container-battle-dialogue`, `choice-button-*`, `button-next-scene`) stay exactly as they are. The panel's `max-h-[60vh] overflow-y-auto` already absorbs the extra header height.
  </action>
  <verify>
    <automated>npm run check && ! grep -rn "NPCSprite" client/src/components/BattleEncounterScreen.tsx && grep -q "DialoguePortrait" client/src/components/BattleEncounterScreen.tsx && grep -q "npc-battle-sprite" client/src/components/DialoguePortrait.tsx && grep -c "data-testid" client/src/components/BattleEncounterScreen.tsx | grep -q "[4-9]"</automated>
  </verify>
  <done>BattleEncounterScreen renders DialoguePortrait in the header; NPCSprite import gone; dialogue/choices/feedback JSX and handlers unchanged (git diff confined to the import line + header block).</done>
</task>

<task type="auto">
  <name>Task 3: Delete NPCSprite.tsx + final regression sweep</name>
  <files>client/src/components/NPCSprite.tsx, client/src/phaser/SpriteFactory.ts</files>
  <action>
1. Confirm zero remaining consumers: `grep -rn "NPCSprite" client/src --include="*.ts" --include="*.tsx"` must show only NPCSprite.tsx itself and the SpriteFactory.ts comment. Then `git rm client/src/components/NPCSprite.tsx`.
2. Update the SpriteFactory.ts line-32 comment from "(matches NPCSprite.tsx SVG art)" to "(legacy SVG palette — NPCSprite.tsx deleted in Phase 25; dialogue portraits now crop the PNG sheets via spriteAssetPaths)". No code changes in SpriteFactory.ts — comment only.
3. Regression sweep:
   - `npm run check` (tsc clean)
   - `npm run build` (production build clean)
   - `grep -rn "NPCSprite" client/src` returns nothing
   - `git diff` for GameContainer.tsx, UnifiedGamePage.tsx, ExplorationScene.ts is empty (zero-regression criterion)
4. Note for the SUMMARY: live look-check of the portrait in actual dialogue is deferred to the user (established v2.2/v2.3 pattern) — include a 30-second playthrough script: Start Full Game -> walk to Riley in Reception -> talk -> confirm 96px breathing receptionist portrait + name plate; then talk to an ER NPC to confirm a different sheet resolves.
  </action>
  <verify>
    <automated>npm run check && npm run build && ! grep -rn "NPCSprite" client/src --include="*.ts" --include="*.tsx" && git diff --quiet client/src/components/GameContainer.tsx client/src/pages/UnifiedGamePage.tsx client/src/phaser/scenes/ExplorationScene.ts</automated>
  </verify>
  <done>NPCSprite.tsx deleted; no references remain anywhere in client/src; tsc + production build pass; GameContainer/UnifiedGamePage/ExplorationScene untouched.</done>
</task>

</tasks>

<verification>
- `npm run check` and `npm run build` both pass.
- `grep -rn "NPCSprite" client/src` → no matches (placeholder fully gone, VIS-01).
- `grep -n "DialoguePortrait" client/src/components/BattleEncounterScreen.tsx` → import + render.
- `grep -n "portrait-breathe" client/src/components/DialoguePortrait.tsx` → idle animation present (VIS-03).
- `git diff client/src/components/GameContainer.tsx` → empty (zero dialogue-logic regression).
</verification>

<success_criteria>
- Dialogue overlay shows a >=96px pixelated frame-0 crop of the speaking NPC's BootScene-preloaded sheet, in a framed plate with the NPC's name and a 2.4s breathing bob.
- NPCSprite SVG placeholder appears nowhere; the file is deleted.
- Dialogue flow, choices, feedback, scoring, and all existing data-testids behave exactly as before.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dialogue-portraits/25-02-SUMMARY.md` (include the live playthrough script from Task 3).
</output>
