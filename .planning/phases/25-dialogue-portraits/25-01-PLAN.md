---
phase: 25-dialogue-portraits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - client/src/data/roomData.json
  - client/src/data/spriteAssetPaths.ts
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "Every named NPC in roomData.json carries a sprite field whose value is one of the 9 sheet types BootScene preloads"
    - "getNPCPortraitPath(npcId) returns the correct PNG sheet path for all 26 named NPCs"
    - "An unknown npcId falls back to the staff sheet AND logs a console.warn in dev mode — never a silent generic fallback"
    - "VIS-01, VIS-02, VIS-03 are defined in REQUIREMENTS.md with traceability rows"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v2.3 Requirements section defining VIS-01..03"
      contains: "VIS-01"
    - path: "client/src/data/roomData.json"
      provides: "sprite field on all 26 named NPCs"
    - path: "client/src/data/spriteAssetPaths.ts"
      provides: "Data-driven npcId -> sheet path resolver"
      exports: ["getNPCPortraitPath"]
  key_links:
    - from: "client/src/data/spriteAssetPaths.ts"
      to: "client/src/data/roomData.json"
      via: "module-level index built from rooms[].npcs[].sprite"
      pattern: "roomData|rooms.*npcs"
    - from: "getNPCPortraitPath"
      to: "SPONSOR_SPRITE_PATHS"
      via: "npc_${sprite}_sheet key lookup"
      pattern: "npc_\\$\\{.*\\}_sheet|_sheet"
---

<objective>
Define the VIS-01..03 requirements (marked TBD in REQUIREMENTS.md) and build the data-driven NPC portrait foundation: every named NPC in roomData.json gets a `sprite` type field, and spriteAssetPaths.ts gains a `getNPCPortraitPath(npcId)` resolver that maps any dialogue speaker to the PNG spritesheet BootScene preloads — with a dev-mode warning on fallback.

Purpose: Phase 25 success criterion 2 — "Every named NPC in roomData.json resolves to the correct spritesheet via a data-driven mapping (no hardcoded component map; named characters never silently fall back to a generic sheet)." This plan supplies the data and the resolver; Plan 02 consumes them in the dialogue UI.
Output: REQUIREMENTS.md v2.3 section, sprite-annotated roomData.json, extended spriteAssetPaths.ts.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/data/spriteAssetPaths.ts
@client/src/data/roomData.json

Investigation findings (verified 2026-06-10, do not re-derive):
- roomData.json shape: `{ "rooms": [ { id, npcs: [...] } ] }` — 12 rooms, 26 named NPCs total. Only 4 NPCs currently carry a `sprite` field (riley_entrance: "receptionist", aiyana_intake: "officer", marcus_lab_aide: "staff", dr_tovar: "officer").
- roomData.json is imported only by UnifiedGamePage.tsx, typed loosely (`npcs: any[]`) — adding a field breaks no types.
- BootScene preloads exactly 9 NPC sheets, keys `npc_<type>_sheet` for types: receptionist, nurse, doctor, it_tech, officer, boss, staff, patient, visitor. Sheets are 96x128 PNG, 3x4 grid of 32x32 frames, frame 0 = idle-down (top-left).
- spriteAssetPaths.ts already exports `SPONSOR_SPRITE_PATHS` (all 9 sheet keys -> paths) and `getSponsorSpritePath` (used by CertificateOverlay — must remain untouched).
- The Phaser in-world NPC textures come from procedural SpriteFactory art (`npcTextureKey`), NOT these PNG sheets. That id-map encodes prior intent for which character type each NPC is — the sprite assignments below mirror it. Do NOT modify SpriteFactory.ts or ExplorationScene.ts in this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define VIS-01..03 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add a new section `## v2.3 Requirements (Active — Nintendo Polish)` between the v2.2 requirements block and `## Out of Scope`, following the SORTV2/TRIA precedent format (checkbox + bold ID + full behavioral spec per line). Define exactly these three (leave VIS-04..08 for Phases 26/27 to define — add a one-line note saying so):

- [ ] **VIS-01**: The dialogue overlay (BattleEncounterScreen) renders the speaking NPC as a >=96px pixelated portrait CSS-cropped from frame 0 (idle-down) of the same `npc_<type>_sheet` PNG BootScene preloads (Phase 21 CertificateOverlay / Phase 24 NPCReactionBubble crop pattern). The NPCSprite SVG placeholder no longer appears anywhere in dialogue, and NPCSprite.tsx is deleted once it has zero consumers.
- [ ] **VIS-02**: Every named NPC in roomData.json carries a `sprite` type field (one of the 9 BootScene sheet types), and dialogue portrait resolution is data-driven via a `getNPCPortraitPath(npcId)` resolver in spriteAssetPaths.ts built from that data — no hardcoded component-level npcId map. Unknown/unmapped npcIds fall back to the staff sheet AND emit a `console.warn` in dev mode — named characters never silently render generic.
- [ ] **VIS-03**: The portrait sits in a framed plate with the NPC's name and shows a subtle idle animation (breathing bob, ~2.4s loop, 2-3px amplitude) while dialogue is on screen (Commandment 4 — NPCs are people, not icons). Dialogue flow, choices, feedback, and scoring behave exactly as before — zero regression to GameContainer logic; `npm run check` and `npm run build` clean.

Also append three rows to the `## Traceability` table: VIS-01 / VIS-02 / VIS-03, all `Phase 25`, status `Pending`.
  </action>
  <verify>
    <automated>grep -c "VIS-0[123]" .planning/REQUIREMENTS.md  # expect >= 6 (3 definitions + 3 traceability rows)</automated>
  </verify>
  <done>REQUIREMENTS.md contains a v2.3 section with VIS-01..03 fully specified in SORTV2-precedent format plus traceability rows; no other requirement text altered.</done>
</task>

<task type="auto">
  <name>Task 2: Add sprite field to all 26 named NPCs in roomData.json</name>
  <files>client/src/data/roomData.json</files>
  <action>
Add a `"sprite"` field to every NPC in every room's `npcs` array that doesn't already have one. Use EXACTLY these assignments (mirrors SpriteFactory's npcTextureKey intent so portraits match established character identity; the 4 existing fields are already correct — leave them):

hospital_entrance: riley_entrance="receptionist" (exists), aiyana_intake="officer" (exists)
reception: riley="receptionist", nervous_patient="patient", chatty_visitor="visitor"
er: dr_martinez="doctor", officer="officer", frantic_family="visitor", priya_privacy_officer="officer"
lab: lab_tech="it_tech", marcus_lab_aide="staff" (exists), researcher="doctor", courier="visitor"
records_room: records_clerk="receptionist", patient_request="patient", attorney="boss", compliance_officer="officer", dr_tovar="officer" (exists)
it_office: security_analyst="it_tech", vendor="visitor", workaround_employee="staff"
break_room: gossiping_coworker="nurse", friend_fishing="staff", tired_employee="patient", hr_director="boss", selfie_coworker="visitor"

Place the field next to the existing `sprite` position used by riley_entrance (after `role` / before coordinates is fine — JSON key order is cosmetic). Change NOTHING else in the file: no dialogue, scene, zone, item, or door edits.
  </action>
  <verify>
    <automated>python3 -c "
import json,sys
d=json.load(open('client/src/data/roomData.json'))
valid={'receptionist','nurse','doctor','it_tech','officer','boss','staff','patient','visitor'}
missing=[(r['id'],n['id']) for r in d['rooms'] for n in r.get('npcs',[]) if n.get('sprite') not in valid]
total=sum(len(r.get('npcs',[])) for r in d['rooms'])
assert not missing, f'NPCs missing/invalid sprite: {missing}'
assert total==26, f'expected 26 NPCs, found {total}'
print('OK: all', total, 'NPCs have valid sprite types')"</automated>
  </verify>
  <done>All 26 named NPCs carry a valid sprite type; JSON parses; git diff shows only added "sprite" lines.</done>
</task>

<task type="auto">
  <name>Task 3: Add getNPCPortraitPath resolver to spriteAssetPaths.ts</name>
  <files>client/src/data/spriteAssetPaths.ts</files>
  <action>
Extend spriteAssetPaths.ts (do NOT rename/remove SPONSOR_SPRITE_PATHS or getSponsorSpritePath — CertificateOverlay depends on them):

1. `import roomData from './roomData.json';` and build a module-level index:
   `const NPC_SPRITE_TYPE_BY_ID: Record<string, string>` by iterating `(roomData as { rooms: Array<{ npcs?: Array<{ id: string; sprite?: string }> }> }).rooms` and mapping each npc.id -> npc.sprite (skip entries without sprite — after Task 2 there are none, but be defensive).
2. Export `getNPCPortraitPath(npcId: string): string`:
   - Look up `NPC_SPRITE_TYPE_BY_ID[npcId]` -> build sheet key `npc_${type}_sheet` -> return `SPONSOR_SPRITE_PATHS[sheetKey]` when present.
   - Fallback path (unknown npcId OR type not in SPONSOR_SPRITE_PATHS): `if (import.meta.env.DEV) console.warn(\`[DialoguePortrait] No sprite mapping for NPC id "${npcId}" — falling back to staff sheet. Add a sprite field in roomData.json.\`);` then return `SPONSOR_SPRITE_PATHS.npc_staff_sheet`.
3. Update the file header comment: the map now serves both the Phase 21 sponsor capstone AND Phase 25 dialogue portraits.

Add a short doc comment noting the sheets are 96x128, 3x4 grid, frame 0 (top-left 32x32) = idle-down — the crop consumers rely on this geometry.
  </action>
  <verify>
    <automated>npm run check && grep -q "getNPCPortraitPath" client/src/data/spriteAssetPaths.ts && grep -q "console.warn" client/src/data/spriteAssetPaths.ts && grep -q "getSponsorSpritePath" client/src/data/spriteAssetPaths.ts</automated>
  </verify>
  <done>tsc clean; getNPCPortraitPath exported and resolves via roomData sprite fields with dev-warning staff fallback; getSponsorSpritePath untouched.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (tsc clean).
- Python assertion from Task 2 passes: 26/26 NPCs with valid sprite types.
- `grep -n "VIS-0" .planning/REQUIREMENTS.md` shows definitions + traceability rows.
- `git diff --stat` touches ONLY the three files in files_modified — SpriteFactory.ts, ExplorationScene.ts, CertificateOverlay.tsx untouched.
</verification>

<success_criteria>
- VIS-01..03 defined in REQUIREMENTS.md replacing their TBD status (SORTV2/TRIA precedent honored).
- Data-driven mapping exists end-to-end: roomData.json sprite fields -> getNPCPortraitPath -> BootScene-preloaded sheet path, with loud dev fallback.
- Plan 02 can consume getNPCPortraitPath without touching data files.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dialogue-portraits/25-01-SUMMARY.md`
</output>
