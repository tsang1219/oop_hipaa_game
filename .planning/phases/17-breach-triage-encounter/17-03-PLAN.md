---
phase: 17-breach-triage-encounter
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - shared/schema.ts
  - client/src/data/roomData.json
  - client/src/phaser/scenes/ExplorationScene.ts
  - client/src/phaser/SpriteFactory.ts
  - client/src/phaser/EventBridge.ts
  - client/src/pages/UnifiedGamePage.tsx
  - .planning/HIPAA_TRAINING_FRAMEWORK.md
autonomous: true
requirements: [TRIA-01, TRIA-06]

must_haves:
  truths:
    - "In the full game at Act 3, Priya the Privacy Officer appears in the ER; pressing SPACE on her opens the EncounterRequestModal; accepting opens the Breach Triage overlay with ExplorationScene paused in place"
    - "Priya does NOT appear in demo mode or before Act 3; ER completion requirements are unchanged (encounter is optional)"
    - "Completing the encounter shows TriageDebrief (accuracy + avg response + takeaways); dismissing it returns control immediately, writes the registry guard so the encounter does not re-fire, and the unified compliance score increases by the contribution"
    - "Declining or aborting (Esc/X) resumes exploration without a registry write — the encounter stays replayable"
    - "Phase 13 TD and Phase 16/22 PHI Sorter encounter flows are unregressed"
    - "HIPAA_TRAINING_FRAMEWORK.md Part 3 reflects scenario-tested Breach Notification coverage (§3.3 STRONG, 500+ threshold gap closed)"
  artifacts:
    - path: "client/src/data/roomData.json"
      provides: "priya_privacy_officer NPC in er room with breach-triage encounterTrigger"
      contains: "breach-triage-er"
    - path: "shared/schema.ts"
      provides: "encounterTrigger schema extended with optional encounterType + minAct"
      contains: "encounterType"
    - path: "client/src/pages/UnifiedGamePage.tsx"
      provides: "breach-triage encounter phase branch + handleTriageComplete + TriageDebrief discriminator"
      contains: "BreachTriageOverlay"
    - path: ".planning/HIPAA_TRAINING_FRAMEWORK.md"
      provides: "Updated Part 3 coverage ratings + revision history row"
      contains: "Breach Triage"
  key_links:
    - from: "client/src/data/roomData.json"
      to: "client/src/phaser/scenes/ExplorationScene.ts"
      via: "npc.encounterTrigger interception in triggerInteraction emits ENCOUNTER_REQUEST with encounterType"
      pattern: "encounterType"
    - from: "client/src/pages/UnifiedGamePage.tsx"
      to: "client/src/components/breach-triage/BreachTriageOverlay.tsx"
      via: "encounterPhase === 'breach-triage' render branch"
      pattern: "encounterPhase === 'breach-triage'"
    - from: "client/src/pages/UnifiedGamePage.tsx"
      to: "useGameState"
      via: "handleTriageComplete → gameState.addScore + recordEncounterResult"
      pattern: "addScore\\(result.scoreContribution"
---

<objective>
Wire the Breach Triage encounter end-to-end: schema + roomData NPC trigger (act-gated, demo-excluded), ExplorationScene passthrough, UnifiedGamePage phase machine + debrief discrimination + score feed, and the HIPAA Training Framework rating update.

Purpose: Roadmap success criteria 1 and 6 — the encounter launches from the Act 3 ER narrative arc through the existing encounter lifecycle, and Breach Notification coverage becomes scenario-tested.
Output: Fully playable encounter; framework doc updated; clean build.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-phi-sorter-encounter/16-04-SUMMARY.md (the encounter lifecycle this plan mirrors — NPC trigger, registry guard, abort path, debrief discrimination)
@shared/schema.ts (npcSchema.encounterTrigger — currently { encounterId, documentSetId, requestText })
@client/src/phaser/scenes/ExplorationScene.ts (triggerInteraction ~line 2648: encounterTrigger interception; NPC spawn loop ~line 931; getCurrentAct ~line 2163; onReturnFromEncounter ~line 2242 already generic)
@client/src/pages/UnifiedGamePage.tsx (encounter lifecycle ~lines 950-1105, render branches ~lines 1504-1585, SORTER_LOCATION_LABELS ~line 59)
@client/src/phaser/SpriteFactory.ts (npcTextureKey map ~line 3769 — unmapped ids fall back to npc_staff)
@client/src/data/triageData.ts (Plan 01)
@client/src/components/breach-triage/BreachTriageOverlay.tsx and TriageDebrief.tsx (Plan 02)

<interfaces>
From Plan 02 (use exactly — do not modify the components):
```typescript
<BreachTriageOverlay
  incidentSetId={string}    // 'breach-triage-set-1'
  encounterId={string}      // 'breach-triage-er'
  onComplete={(result: { encounterId: string; correctCount: number; totalCount: number;
                         scoreContribution: number; avgResponseMs: number;
                         takeaways: [string, string] }) => void}
  onAbort={() => void}
/>
<TriageDebrief encounterId correctCount totalCount avgResponseMs scoreContribution takeaways locationLabel onDismiss />
```
Existing generic plumbing (verified — needs NO changes): onReturnFromEncounter writes `encounterResult_${encounterId}` registry guard for any encounterId; triggerInteraction's `alreadyDone` check suppresses completed encounters; EncounterRequestModal is presentational and encounter-agnostic; decline path emits REACT_RETURN_FROM_ENCOUNTER { aborted: true }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + roomData NPC + Phaser-side gating and passthrough</name>
  <files>shared/schema.ts, client/src/data/roomData.json, client/src/phaser/scenes/ExplorationScene.ts, client/src/phaser/SpriteFactory.ts, client/src/phaser/EventBridge.ts</files>
  <action>
1. **shared/schema.ts** — extend `npcSchema.encounterTrigger` with two optional fields:
   - `encounterType: z.enum(['phi-sorter', 'breach-triage']).optional()` — discriminator; absent = 'phi-sorter' (backward compat with the three existing sorter NPCs)
   - `minAct: z.number().optional()` — minimum act for the NPC to spawn at all
   Add a comment that `documentSetId` is the generic content-set key (sorter document set OR triage incident set).

2. **client/src/data/roomData.json** — add a new NPC to the `er` room's npcs array (do NOT touch `completionRequirements` — the encounter is optional, Dr. Tovar precedent):
   ```json
   {
     "id": "priya_privacy_officer",
     "name": "Priya",
     "role": "Privacy Officer",
     "x": 8, "y": 12,
     "sceneId": "_encounter_breach_triage_er",
     "encounterTrigger": {
       "encounterId": "breach-triage-er",
       "documentSetId": "breach-triage-set-1",
       "encounterType": "breach-triage",
       "minAct": 3,
       "requestText": "<write in Priya's voice — exhausted Privacy Officer camped at the triage desk mid-breach-crisis, incident queue overflowing, needs help deciding what's reportable before OCR's office opens. 2-3 sentences, Nintendo Test tone.>"
     },
     "isFinalBoss": false
   }
   ```
   Tile (8,12) is verified free in the ER layout (triage_desk occupies 8-10,13; equipment at 5-6,12 and 13-14,12; player spawn 10,12).

3. **client/src/phaser/scenes/ExplorationScene.ts** — two edits:
   - NPC spawn loop (`for (const npc of room.npcs)` ~line 931): at the top of the loop body, `continue` when `npc.encounterTrigger?.minAct` is set AND (`this.getCurrentAct() < minAct` OR `isDemoActive()`). isDemoActive is already imported. This keeps Priya out of demo mode (demo visits the ER in Act 1 state) and out of pre-Act-3 full-game ER visits.
   - encounterTrigger interception in `triggerInteraction` (~line 2689): add `encounterType: npc.encounterTrigger.encounterType` to the ENCOUNTER_REQUEST emit payload.

4. **client/src/phaser/SpriteFactory.ts** — add `priya_privacy_officer: 'npc_officer'` to the npcTextureKey map (the compliance_officer mapping precedent; without it she falls back to npc_staff).

5. **client/src/phaser/EventBridge.ts** — update the ENCOUNTER_REQUEST JSDoc payload shape to include `encounterType?: 'phi-sorter' | 'breach-triage'` (comment-only change; no new event constants — matches the Phase 16-02 payload-extension precedent).
  </action>
  <verify>
    <automated>npm run check && npx tsx -e "
import { roomDataSchema } from './shared/schema';
import data from './client/src/data/roomData.json';
const rooms: any[] = (data as any).rooms;
const er = rooms.find(r => r.id === 'er');
const p = er.npcs.find((n: any) => n.id === 'priya_privacy_officer');
if (!p) throw new Error('priya missing');
if (p.encounterTrigger?.encounterType !== 'breach-triage') throw new Error('encounterType wrong');
if (p.encounterTrigger?.minAct !== 3) throw new Error('minAct wrong');
if (er.completionRequirements.requiredNpcs.includes('priya_privacy_officer')) throw new Error('must stay optional');
console.log('roomData OK');
" 2>/dev/null || npx tsx -e "
import data from './client/src/data/roomData.json';
const er = (data as any).rooms.find((r: any) => r.id === 'er');
const p = er.npcs.find((n: any) => n.id === 'priya_privacy_officer');
if (!p || p.encounterTrigger?.encounterType !== 'breach-triage' || p.encounterTrigger?.minAct !== 3) throw new Error('NPC wiring wrong');
if (er.completionRequirements.requiredNpcs.includes('priya_privacy_officer')) throw new Error('must stay optional');
console.log('roomData OK');
"</automated>
  </verify>
  <done>Schema validates the new fields; Priya exists in ER with the breach-triage trigger, act-gated and demo-excluded at spawn time; ENCOUNTER_REQUEST carries encounterType; sprite maps to npc_officer; ER completion requirements untouched.</done>
</task>

<task type="auto">
  <name>Task 2: UnifiedGamePage wiring — phase branch, completion handler, debrief discrimination</name>
  <files>client/src/pages/UnifiedGamePage.tsx</files>
  <action>
All edits in client/src/pages/UnifiedGamePage.tsx, mirroring the Phase 16 sorter wiring:

1. Extend `EncounterPhase` union (~line 221) with `'breach-triage'`.
2. Extend the `encounterRequest` state type and the `onEncounterRequest` handler payload (~line 987) with `encounterType?: 'phi-sorter' | 'breach-triage'`.
3. Extend `narrativeCardData` state type: `type` union gains `'breach-triage'`; add optional `triageConfig?: { incidentSetId: string }`.
4. `handleAcceptEncounterRequest` (~line 1011): branch on `encounterRequest.encounterType === 'breach-triage'` → set narrativeCardData `{ narrativeText: '', encounterId, type: 'breach-triage', triageConfig: { incidentSetId: documentSetId } }` and `setEncounterPhase('breach-triage')`; else existing phi-sorter behavior unchanged.
5. Extend `encounterResult` state type with `avgResponseMs?: number` and `kind?: 'triage'` (leave sorter/TD results untouched — only triage sets `kind`).
6. New `handleTriageComplete` callback mirroring `handleSorterComplete`: import `getTriageIncidentSet` from '@/data/triageData'; accuracy = correctCount/totalCount; outcome vs `set.passingAccuracy ?? 0.7`; `setEncounterResult({ encounterId, outcome, securityScore: Math.round(accuracy*100), scoreContribution, takeaways, correctCount, totalCount, avgResponseMs: result.avgResponseMs, kind: 'triage' })`; `setEncounterPhase('debrief')`; `gameState.addScore(result.scoreContribution)` when > 0; `gameState.recordEncounterResult(result.encounterId, { completed: true, score: result.correctCount, outcome })`.
7. Render branch after the phi-sorter branch (~line 1541): `{encounterPhase === 'breach-triage' && narrativeCardData?.triageConfig && (<BreachTriageOverlay incidentSetId={...} encounterId={narrativeCardData.encounterId} onComplete={handleTriageComplete} onAbort={handleSorterAbort} />)}` — reuse `handleSorterAbort` verbatim (it is encounter-agnostic: resets phase, emits aborted:true, no registry write).
8. Debrief discrimination (~line 1563): render `TriageDebrief` when `encounterResult.kind === 'triage'` (passing avgResponseMs, locationLabel, onDismiss={handleDismissDebrief}); keep SorterDebrief for `takeaways && kind !== 'triage'`; EncounterDebrief otherwise. handleDismissDebrief needs NO changes (BLOCKER-3-safe id capture + registry-guard emit are generic).
9. Add `'breach-triage-er': 'PRIYA'` to SORTER_LOCATION_LABELS (~line 59) so the close button reads "BACK TO PRIYA".

Regression guards: do not modify handleConfirmNarrativeCard, the TD branches, handleSorterComplete, or onEncounterTriggered defaults — phi-sorter and TD flows must remain byte-identical in behavior.
  </action>
  <verify>
    <automated>npm run check && grep -q "encounterPhase === 'breach-triage'" client/src/pages/UnifiedGamePage.tsx && grep -q "kind === 'triage'" client/src/pages/UnifiedGamePage.tsx && grep -q "breach-triage-er" client/src/pages/UnifiedGamePage.tsx</automated>
  </verify>
  <done>Accept on Priya routes to the breach-triage phase; completion feeds the unified score and shows TriageDebrief; abort/decline replayable; sorter + TD paths untouched.</done>
</task>

<task type="auto">
  <name>Task 3: HIPAA framework rating update + full build verification</name>
  <files>.planning/HIPAA_TRAINING_FRAMEWORK.md</files>
  <action>
1. Update `.planning/HIPAA_TRAINING_FRAMEWORK.md` Part 3 (Breach Notification Rule). IMPORTANT context: the roadmap's "THIN → ADEQUATE" phrasing predates the 2026-03-11 correction pass that already raised §3.3 to ADEQUATE — the intent of TRIA-06 is "Breach Notification becomes scenario-tested." Therefore:
   - **§3.1 What Constitutes a Breach** → `STRONG`. Add game content bullets: Breach Triage encounter (triageData.ts) — encryption safe harbor (encrypted-laptop), good-faith workforce exception (good-faith-glance), 50-year deceased rule (deceased-records), ransomware presumption (ransomware), snooping/misuse as breach (hr-snooping, internal-misuse).
   - **§3.2 Breach Response Procedures** → keep `ADEQUATE`, add a bullet noting the triage encounter drills classify-then-notify decisions under time pressure.
   - **§3.3 Notification Requirements** → `STRONG`. Add bullets: 60-day individual notice, >500 media + concurrent OCR notice, <500 annual log, BA→CE 60-day notice — all scenario-tested with wrong-answer feedback in the Breach Triage encounter. DELETE the "Remaining gap" line about the 500+ threshold (now covered). Note state-AG notification remains out of scope (state-level, per framework scope statement).
   - Update the Coverage Summary table counts/topic lists to match the new ratings.
   - Add a Revision History row dated with today's date: "Phase 17: Breach Triage encounter added (triageData.ts, 9 scenario-tested incidents). §3.1 ADEQUATE→STRONG, §3.3 ADEQUATE→STRONG (500+ threshold gap closed). Roadmap TRIA-06 'THIN→ADEQUATE' target exceeded."
2. Run full verification: `npm run check` then `npm run build` — both must pass clean.
  </action>
  <verify>
    <automated>npm run check && npm run build && grep -q "Breach Triage" .planning/HIPAA_TRAINING_FRAMEWORK.md && ! grep -q "Remaining gap:.*500" .planning/HIPAA_TRAINING_FRAMEWORK.md</automated>
  </verify>
  <done>Framework Part 3 reflects scenario-tested coverage with updated summary table and revision history; tsc + vite build clean.</done>
</task>

</tasks>

<verification>
- `npm run check` and `npm run build` pass
- Grep wiring chain: roomData breach-triage-er → ExplorationScene encounterType passthrough → UnifiedGamePage breach-triage branch → BreachTriageOverlay → handleTriageComplete → TriageDebrief
- Regression greps: `phi-sorter` branch and TD `REACT_LAUNCH_ENCOUNTER` paths unchanged (git diff of UnifiedGamePage shows only additive changes around the listed line ranges)
- Manual playthrough (DEFERRED TO USER, note in SUMMARY — do not block): Act 3 full game → ER → SPACE on Priya → accept → keyboard-only run → debrief → walk immediately after dismiss → no re-fire on return; demo mode ER shows no Priya
</verification>

<success_criteria>
Roadmap success criteria 1 and 6 land: the Act 3 ER trigger launches the encounter through the existing lifecycle with ExplorationScene paused in place, the debrief feeds the unified compliance score proportionally, and the HIPAA Training Framework's Breach Notification coverage is upgraded to scenario-tested. TD and PHI Sorter flows unregressed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-breach-triage-encounter/17-03-SUMMARY.md`
</output>
