---
phase: 17-breach-triage-encounter
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - client/src/components/breach-triage/TriageIncidentCard.tsx
  - client/src/components/breach-triage/TriageFollowUpPanel.tsx
  - client/src/components/breach-triage/TriageDebrief.tsx
  - client/src/components/breach-triage/BreachTriageOverlay.tsx
autonomous: true
requirements: [TRIA-02, TRIA-03, TRIA-04, TRIA-06]

must_haves:
  truths:
    - "Up to 3 incidents are visible simultaneously, each with its own depleting timer bar, spawning at an escalating cadence"
    - "Keyboard-only completion works end-to-end: 1/2/3 focus a slot, R/N classify, 1/2/3 answer follow-ups, Esc aborts — no mouse required at any step"
    - "Correct classification of a reportable incident opens the two-step notify/timeline follow-up; wrong answers show the rule explanation"
    - "Every interaction has paired audio + visual feedback: ding+green pulse, buzz+red flash, urgency cue on time-up; vignette pulses when 2+ incidents are on the board"
    - "After the last incident resolves, a 600ms beat precedes the fanfare and onComplete fires with correctCount/totalCount/avgResponseMs/takeaways"
  artifacts:
    - path: "client/src/components/breach-triage/BreachTriageOverlay.tsx"
      provides: "Game loop: spawn engine, per-incident timers, hotkeys, feedback, follow-up orchestration, completion"
      exports: ["BreachTriageOverlay"]
      min_lines: 250
    - path: "client/src/components/breach-triage/TriageIncidentCard.tsx"
      provides: "Single incident slot card with timer bar, focus ring, classify buttons"
    - path: "client/src/components/breach-triage/TriageFollowUpPanel.tsx"
      provides: "Two-step notify/timeline question panel with number-key answers"
    - path: "client/src/components/breach-triage/TriageDebrief.tsx"
      provides: "Triage completion screen: accuracy bar + avg response time + takeaways"
      exports: ["TriageDebrief"]
  key_links:
    - from: "client/src/components/breach-triage/BreachTriageOverlay.tsx"
      to: "client/src/data/triageData.ts"
      via: "getTriageIncidentSet(incidentSetId)"
      pattern: "getTriageIncidentSet"
    - from: "client/src/components/breach-triage/BreachTriageOverlay.tsx"
      to: "Phaser sound manager"
      via: "eventBridge.emit(BRIDGE_EVENTS.REACT_PLAY_SFX, ...)"
      pattern: "REACT_PLAY_SFX"
---

<objective>
Build the Breach Triage overlay UI: a 3-slot whack-a-mole incident board with per-incident countdown timers, escalating spawn cadence, full keyboard control, the two-step notification follow-up, proportional audio/visual feedback, and a purpose-built TriageDebrief.

Purpose: This is the game itself — roadmap success criteria 2, 3, 4, and the 60-120s pacing of criterion 6. Pure React components; Plan 03 mounts them in UnifiedGamePage.
Output: Four components in client/src/components/breach-triage/.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/data/triageData.ts (created by Plan 01 — the content contract)
@client/src/components/phi-sorter/PHISorterOverlay.tsx (the pattern to follow: phase state machine, REACT_PLAY_SFX, 600ms anticipation beat, Esc abort, error fallback for bad set ID)
@client/src/components/phi-sorter/SorterDebrief.tsx (visual language for TriageDebrief: Press Start 2P, accuracy bar, band colors, close button)
@client/src/phaser/EventBridge.ts (BRIDGE_EVENTS constants)

<interfaces>
<!-- Contracts the executor builds against. From Plan 01's triageData.ts: -->
```typescript
export type TriageOption = { id: string; label: string; correct: boolean; explanation: string };
export type TriageFollowUp = {
  notifyPrompt: string; notifyOptions: TriageOption[];      // 3 options, 1 correct
  timelinePrompt: string; timelineOptions: TriageOption[];  // 3 options, 1 correct
};
export type TriageIncident = {
  id: string; headline: string; detail: string; reportable: boolean;
  classificationExplanation: string; followUp?: TriageFollowUp; difficulty: 1 | 2 | 3;
};
export type TriageIncidentSet = {
  id: string; npcId: string; npcName: string; npcRole: string;
  contextCard: { title: string; body: string };
  incidents: TriageIncident[]; passingAccuracy: number; takeaways: [string, string];
};
export function getTriageIncidentSet(id: string): TriageIncidentSet | undefined;
```

<!-- The overlay's own public contract (Plan 03 consumes this — do not deviate): -->
```typescript
export type BreachTriageOverlayProps = {
  incidentSetId: string;   // 'breach-triage-set-1'
  encounterId: string;     // 'breach-triage-er' — round-trips into onComplete
  onComplete: (result: {
    encounterId: string;
    correctCount: number;        // correct classifications + correct follow-up answers
    totalCount: number;          // classifications (9) + 2 per follow-up shown
    scoreContribution: number;   // Math.round((correctCount/totalCount) * 12), 0..12
    avgResponseMs: number;       // mean spawn→classification time over player-made classifications (0 if none)
    takeaways: [string, string]; // pass-through from set.takeaways
  }) => void;
  onAbort?: () => void;    // Esc / X — no scoring, caller handles replayability
};
```

<!-- SFX keys — ALL already preloaded in BootScene, do NOT add new preloads: -->
- correct: { key: 'sfx_sorter_correct', volume: 0.7 }
- wrong/miss: { key: 'sfx_sorter_wrong', volume: 0.7 }
- incident spawn: { key: 'sfx_interact', volume: 0.3 }
- time-up / board-full urgency: { key: 'sfx_breach_alert', volume: 0.25 }
- completion fanfare: { key: 'sfx_fanfare', volume: 0.7 }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Presentational components — TriageIncidentCard, TriageFollowUpPanel, TriageDebrief</name>
  <files>client/src/components/breach-triage/TriageIncidentCard.tsx, client/src/components/breach-triage/TriageFollowUpPanel.tsx, client/src/components/breach-triage/TriageDebrief.tsx</files>
  <action>
Create `client/src/components/breach-triage/` with three presentational components. Visual language matches the sorter family: 'Press Start 2P' font, 4px borders, dark panel backgrounds (#1a1a2e / #2a2a3e), Tailwind classes (inline `style` only for font-family/font-size, like SorterDebrief).

**TriageIncidentCard** — props: `{ slotNumber: 1|2|3; incident: TriageIncident; remainingMs: number; totalMs: number; focused: boolean; feedback: 'correct'|'wrong'|'expired'|null; onSelect: () => void; onClassify: (reportable: boolean) => void }`.
- Styled like an incident ticket: slot-number badge ("[1]"), `headline` in alarm-amber, `detail` below in smaller white text.
- Timer bar across the bottom: width = remainingMs/totalMs; green > 50%, yellow 25-50%, red + pulse animation < 25%.
- `focused` → gold ring (ring-4 ring-yellow-400) like the sorter's keyboard selection.
- Two buttons at the card foot: "[R] REPORTABLE" (red theme) and "[N] NOT REPORTABLE" (teal theme) — clicking calls onClassify; clicking the card body calls onSelect.
- `feedback` drives a brief overlay state: 'correct' = green flash pulse, 'wrong' = red shake, 'expired' = red dim + "TOO SLOW" stamp. Reuse the existing flash-green/shake-red CSS animation classes if they exist globally (check `client/src/index.css` for the Phase 16 keyframes; add component-scoped equivalents via Tailwind `animate-` arbitrary values or a small `<style>` block only if missing).

**TriageFollowUpPanel** — props: `{ incident: TriageIncident; step: 'notify'|'timeline'; wrongPick: TriageOption | null; onPick: (option: TriageOption) => void }`.
- Centered modal panel over a dimmed board ("INCIDENT FLAGGED REPORTABLE — NOW WHAT?"). Shows incident headline for context, then the current step's prompt + 3 options labeled [1] [2] [3].
- When `wrongPick` is set, render the picked option struck-through in red with its `explanation` below in an amber teaching box (this is the §164.404-410 lesson moment — give it room, ≥3s dwell handled by the overlay).
- Options are buttons (mouse) but the panel renders the number-key hints prominently — keyboard parity is the primary path.

**TriageDebrief** — props: `{ encounterId: string; correctCount: number; totalCount: number; avgResponseMs: number; scoreContribution: number; takeaways: string[]; locationLabel?: string; onDismiss: () => void }`.
- Clone SorterDebrief's structure (entrance animation, accuracy bar with band colors at 80/60 thresholds, +N COMPLIANCE score line, KEY LEARNINGS list, close button "BACK TO {locationLabel ?? 'THE ER'}").
- Header text bands: "QUEUE CLEARED" (≥80%), "SHIFT SURVIVED" (60-79%), "OCR IS CALLING" (<60%).
- Add one triage-specific stat row the sorter doesn't have: "AVG RESPONSE: X.Xs" (avgResponseMs/1000, one decimal).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Three components compile with the exact prop contracts above; TriageDebrief shows accuracy + avg response + takeaways; card shows timer bar, focus ring, and R/N affordances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BreachTriageOverlay — spawn engine, hotkeys, feedback, completion</name>
  <files>client/src/components/breach-triage/BreachTriageOverlay.tsx</files>
  <behavior>
- Incidents spawn into free slots (max 3) in triageData order; spawn delay for the i-th spawn = max(2000, 3500 - i*200) ms; first incident spawns ~600ms after mount (let the board breathe — Commandment 2)
- Per-incident timer: difficulty 1 = 14000ms, 2 = 12000ms, 3 = 10000ms; expiry counts the incident as an incorrect decision and frees the slot
- All timers AND the spawner freeze while a follow-up panel or a wrong-answer explanation toast is visible (the player is reading a lesson — never punish reading)
- R/N on a reportable incident: R = correct → if followUp exists, open TriageFollowUpPanel; N = wrong → buzz + red shake + classificationExplanation toast (3000ms dwell)
- R on a NOT-reportable incident = wrong (false alarm) → buzz + explanation toast; N = correct → ding + green pulse, slot frees
- Follow-up: number keys 1/2/3 (and clicks) commit an option directly; wrong pick → buzz + explanation shown in-panel 3000ms, then advance to next step/close (decision recorded as wrong); correct pick → ding, advance; each answered question increments totalCount
- Run ends when all 9 incidents are resolved or expired → 600ms silent beat → sfx_fanfare 0.7 → onComplete with { encounterId, correctCount, totalCount, scoreContribution: Math.round((correctCount/totalCount)*12), avgResponseMs, takeaways }
- Esc (and an X button top-right) calls onAbort — matches sorter abort UX
  </behavior>
  <action>
Create `BreachTriageOverlay.tsx` exporting `BreachTriageOverlay` with the props contract from the interfaces block. Follow PHISorterOverlay's skeleton: `useMemo(() => getTriageIncidentSet(incidentSetId))`, graceful error fallback panel on unknown set ID (DISMISS button calling onComplete with zeros), full-screen `absolute inset-0 z-40 bg-black/85` layout.

**Layout:** Header strip — "INCIDENT TRIAGE · HELPING PRIYA" + processed counter ("4/9 triaged · 3 correct") + X abort button. Board: 3 fixed slot columns (empty slots render a dim "— monitoring —" placeholder so the grid never jumps). Footer: persistent key-hint bar "[1-3] SELECT · [R] REPORTABLE · [N] NOT REPORTABLE · [ESC] LEAVE".

**State machine:** `phase: 'triaging' | 'follow-up' | 'completing'`; `slots: (ActiveIncident | null)[3]` where ActiveIncident = `{ incident, spawnedAt: number, remainingMs: number }`; `focusedSlot`; `queue` (unspawned incidents); decision tallies + `responseTimes: number[]`; `explanationToast: { text } | null`; `followUpState: { incident, step, wrongPick } | null`.

**Timing engine:** single `setInterval` (~100ms tick) driving remainingMs decrements, spawn scheduling, and expiry — gated by a `frozen` derived flag (followUpState !== null || explanationToast !== null || phase !== 'triaging'). Keep all tick logic in one effect with refs for mutable tallies to avoid stale closures (the Phase 22 SUMMARY flagged stale-closure risk on tally state — use refs mirrored to state, or functional updates).

**Hotkeys:** window keydown listener (cleanup on unmount): digits 1-3 focus an occupied slot (in 'triaging') or pick an option (in 'follow-up'); R/N classify the focused slot (auto-focus the OLDEST occupied slot whenever the focused one resolves or none is focused — fast players never need digit keys); Esc → onAbort. Guard all handlers by phase. `preventDefault` on handled keys only.

**Feedback (Commandments 1, 8 — every beat below pairs audio + visual):**
- spawn: sfx_interact 0.3 + card slide-in animation
- correct: sfx_sorter_correct 0.7 + green pulse on card, slot frees after ~350ms
- wrong: sfx_sorter_wrong 0.7 + red shake + explanation toast (bottom-center amber panel, 3000ms, board frozen)
- time-up: sfx_breach_alert 0.25 + red dim + "TOO SLOW" stamp ~1000ms, then slot frees (no explanation dwell — time-up is a pacing failure, not a knowledge failure; counts as wrong)
- escalating tension: when ≥2 slots occupied, render a pulsing red vignette overlay (inset box-shadow animation, pointer-events-none); when all 3 occupied, fire sfx_breach_alert 0.2 once per full-board episode
- completion: 600ms beat (board fades, no sound) → sfx_fanfare 0.7 → call onComplete

**Stats:** record `performance.now() - spawnedAt` on every player classification (not expiries); avgResponseMs = mean (0 if empty). totalCount = 9 + 2×(follow-ups opened); correctCount per behavior spec.

Mount TriageIncidentCard per occupied slot and TriageFollowUpPanel when followUpState is set. Do NOT import TriageDebrief here — UnifiedGamePage owns the debrief render (same separation as PHISorterOverlay/SorterDebrief, BLOCKER-2 precedent).
  </action>
  <verify>
    <automated>npm run check && grep -c "REACT_PLAY_SFX" client/src/components/breach-triage/BreachTriageOverlay.tsx | awk '{exit ($1 >= 5) ? 0 : 1}'</automated>
  </verify>
  <done>Overlay compiles; all five SFX emission points present; keyboard-only path covers focus → classify → follow-up → completion; spawn/timer/freeze engine matches the behavior spec; onComplete payload matches the Plan 03 contract exactly.</done>
</task>

</tasks>

<verification>
- `npm run check` clean
- BreachTriageOverlay exports the exact BreachTriageOverlayProps contract (Plan 03 imports it without changes)
- No imports from UnifiedGamePage or Phaser scenes — pure React + EventBridge only
- Estimated run length sanity check: 9 incidents × ~3-6s handling + ~4-6 follow-ups × ~6-9s ≈ 60-115s (within the 60-120s budget; spawn delays and timer values above were chosen for this window — do not slow them down)
</verification>

<success_criteria>
Roadmap criteria 2 (whack-a-mole + keyboard-only), 3 (notify/timeline follow-up with rule explanations), 4 (proportional audio/visual feedback + escalating tension), and the pacing/debrief-component half of 6 are implemented and type-check, ready for Plan 03 wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/17-breach-triage-encounter/17-02-SUMMARY.md`
</output>
