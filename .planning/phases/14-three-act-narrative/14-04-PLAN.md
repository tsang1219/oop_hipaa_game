---
phase: 14-three-act-narrative
plan: 04
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - client/src/data/gameData.json
  - client/src/pages/PrivacyQuestPage.tsx
autonomous: true
requirements:
  - NARR-04

must_haves:
  truths:
    - "Security Analyst Sam references fax incident handling when the player has set faxIncidentHandled"
    - "Security Analyst Sam references vendor credential choice when vendorAccessGranted is set"
    - "Records Clerk has an Act 2+ ambient line that acknowledges the player's journey"
    - "Default NPC dialogue (no flags set) is unchanged — variants only appear when the flag exists"
    - "No NPC dialogue reads like a compliance document — all variant lines pass the Nintendo Test"
  artifacts:
    - path: "client/src/data/gameData.json"
      provides: "3 new NPC variant scenes"
      contains: "security_analyst_variant_reported"
    - path: "client/src/pages/PrivacyQuestPage.tsx"
      provides: "getSceneIdForNPC helper that routes to variant scenes based on decision flags"
      contains: "getSceneIdForNPC"
  key_links:
    - from: "client/src/pages/PrivacyQuestPage.tsx (onInteractNPC)"
      to: "client/src/data/gameData.json"
      via: "getSceneIdForNPC returns variant sceneId"
      pattern: "getSceneIdForNPC"
---

<objective>
Write 3-5 new NPC variant dialogue scenes in gameData.json and add conditional scene routing in PrivacyQuestPage so NPCs reference the player's earlier choices at key moments.

Purpose: NARR-04 — the game feels like a living world when Sam in the IT Office remembers what you did in the Break Room. Targeted at 2-3 NPCs with 3-5 variant lines total. Decision flags from Plan 02 drive the routing.
Output: New scenes in gameData.json + `getSceneIdForNPC` conditional routing in `onInteractNPC`.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-three-act-narrative/14-02-SUMMARY.md
@.planning/phases/14-three-act-narrative/14-RESEARCH.md

<interfaces>
<!-- DecisionState from Plan 01/02 -->
```typescript
interface DecisionState {
  faxIncidentHandled: 'reported' | 'delayed' | 'ignored' | null;
  vendorAccessGranted: boolean | null;
  breachEncounterPassed: boolean | null;
}
```

<!-- onInteractNPC in PrivacyQuestPage.tsx (existing pattern) -->
```typescript
const onInteractNPC = (data: { npcId: string; npcName: string; sceneId: string }) => {
  setCurrentNPCId(data.npcId);
  setCurrentSceneId(data.sceneId);  // This drives which scene loads
  setPageMode('dialogue');
};
```

<!-- NPC IDs confirmed from roomData.json -->
- security_analyst (IT Office, sceneId drives to their dialogue scene)
- The records clerk NPC ID — read roomData.json to confirm exact ID
- Any Act 2+ NPC who naturally references the player's accumulated experience

<!-- gameData.json scene structure (every scene must follow this shape) -->
```json
{
  "id": "scene_id_here",
  "character": "Character Name",
  "dialogue": "Opening line of dialogue",
  "choices": [
    {
      "text": "Choice text",
      "score": 0,
      "feedback": "Feedback explaining the HIPAA rule"
    }
  ],
  "isEnd": true
}
```
If a variant scene is purely contextual (no new HIPAA teaching moment), it can have minimal choices
or even no choices (just acknowledgment). However, if the NPC normally teaches something, keep the
teaching content — the variant only changes the opening lines, not the HIPAA content.

<!-- Nintendo Test requirement from CLAUDE.md -->
Commandment 4: NPCs are people, not rule-delivery systems.
Commandment 5: Boring is a bug — if it reads like a compliance document, rewrite it.
All variant dialogue must sound like a real person remembering something, not a system reporting state.

<!-- Variant lines from RESEARCH.md (ready to use) -->
Security Analyst Sam — fax reported: "Word travels fast in compliance. I heard about the fax incident — you reported it right away. That instinct to notify? That's exactly what we need more of."
Security Analyst Sam — fax ignored/delayed: "I'm going to level with you. We had a fax incident recently and it wasn't handled by the book. If something like that happens again, you come find me. Same day."
Records Clerk — Act 2+ journey: "You've been all over this hospital today. I've heard about you from Riley, from the lab folks... You're paying attention to the right things."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author NPC variant scenes in gameData.json</name>
  <files>client/src/data/gameData.json</files>
  <action>
Read `client/src/data/gameData.json` to find the existing Security Analyst scene ID (look in roomData.json for security_analyst npcId → sceneId), then read the current scene content to understand the existing teaching structure.

Read `client/src/data/roomData.json` to find the records clerk NPC's exact ID and sceneId.

Add the following new scenes to the end of the `scenes` array in gameData.json. Each variant references the player's past choice and then flows into the same teaching content as the base scene (or a shortened version). Do not modify existing scenes.

**Scene 1: security_analyst_variant_fax_reported**
```json
{
  "id": "security_analyst_variant_fax_reported",
  "character": "Security Analyst Sam",
  "dialogue": "Word travels fast in compliance. I heard about the fax incident — you reported it right away. That instinct? That's exactly what we need more of around here. Now, about why I called you in...",
  "choices": [
    {
      "text": "Good to know the right call paid off.",
      "score": 0,
      "feedback": "Reporting privacy incidents immediately is exactly right. You start the clock on the 60-day breach notification window and give the organization a chance to manage the risk properly."
    }
  ],
  "isEnd": true
}
```

**Scene 2: security_analyst_variant_fax_delayed_or_ignored**
```json
{
  "id": "security_analyst_variant_fax_delayed_ignored",
  "character": "Security Analyst Sam",
  "dialogue": "Look, I'm going to be straight with you. That fax situation a while back — it wasn't handled by the book. Delays and hoping things blow over are how small incidents become federal violations. If anything like that happens again, you find me. Same day.",
  "choices": [
    {
      "text": "Understood. I'll come to you immediately next time.",
      "score": 3,
      "feedback": "Correct instinct. Breach notification has a hard 60-day limit from discovery to patient notification, but internal reporting should happen within hours — not days. Early reporting means more options to mitigate."
    },
    {
      "text": "It wasn't that serious though, was it?",
      "score": -5,
      "feedback": "It was serious. An unsecured PHI disclosure to an unauthorized recipient triggers HIPAA's breach notification requirements. The severity doesn't reduce your obligation to report — it increases it."
    }
  ],
  "isEnd": true
}
```

**Scene 3: security_analyst_variant_vendor_access**
This variant fires when `vendorAccessGranted === true` (player shared credentials). Sam addresses the credential sharing:
```json
{
  "id": "security_analyst_variant_vendor_credentials",
  "character": "Security Analyst Sam",
  "dialogue": "I need to talk to you about something. Our access logs show your credentials were used by someone from MedTech Solutions. I know they seemed legitimate. But when you share your login, everything they do is logged as you. Every access, every file. You're on the hook.",
  "choices": [
    {
      "text": "I understand. I won't do that again.",
      "score": 5,
      "feedback": "Good. Credential sharing violates HIPAA's access control requirements and creates an audit trail nightmare. Vendors must get their own authorized access through IT — never yours."
    },
    {
      "text": "But they were a legitimate vendor, not a hacker.",
      "score": -5,
      "feedback": "Legitimacy doesn't matter for access logs. Your credentials = your responsibility. Even a legitimate vendor taking one wrong action under your login is a violation traced back to you. Every. Single. Time."
    }
  ],
  "isEnd": true
}
```

**Scene 4: records_clerk_variant_journey**
Fires in Act 2+ (after act1Complete). A warm ambient acknowledgment with minimal score impact:
```json
{
  "id": "records_clerk_variant_journey",
  "character": "Records Clerk Joanna",
  "dialogue": "You know, I've seen a lot of new employees come through here. Most of them treat this floor like a field trip. But you've actually been asking the right questions. Riley mentioned you, the lab team mentioned you. People notice when someone's paying attention.",
  "choices": [
    {
      "text": "Just trying to do right by the patients.",
      "score": 3,
      "feedback": "That instinct — 'doing right by the patients' — is the entire point of HIPAA. It's not a compliance checklist. It's a protection framework for real people. Keep that attitude."
    },
    {
      "text": "Is it that obvious I'm new?",
      "score": 0,
      "feedback": "The questions you ask reveal what you care about. Caring about compliance from day one is exactly the right start."
    }
  ],
  "isEnd": true
}
```

Verify JSON is valid after appending scenes.
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && node -e "const d = JSON.parse(require('fs').readFileSync('client/src/data/gameData.json','utf8')); const ids = ['security_analyst_variant_fax_reported','security_analyst_variant_fax_delayed_ignored','security_analyst_variant_vendor_credentials','records_clerk_variant_journey']; const found = ids.filter(id => d.scenes.find(s => s.id === id)); console.log('Found:', found.length, '/ 4 scenes'); if (found.length < 4) process.exit(1);"</automated>
  </verify>
  <done>All 4 variant scenes exist in gameData.json. JSON is valid. Scene IDs are unique and correctly spelled.</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional scene routing in PrivacyQuestPage onInteractNPC</name>
  <files>client/src/pages/PrivacyQuestPage.tsx</files>
  <action>
Read `client/src/pages/PrivacyQuestPage.tsx`. The `useGameState` hook is already imported (from Plan 02). `decisions` is already available in scope.

**Step 1 — Add a decisionsRef for stale-closure-safe reading:**
Plan 02 already added `useGameState`. Confirm `decisions` from the hook is available. If it is not yet in a ref, add:
```typescript
const decisionsRef = useRef(decisions);
useEffect(() => { decisionsRef.current = decisions; }, [decisions]);
```
This ensures the callback reads current decisions at invocation time, not the closed-over value from mount.

**Step 2 — Add getSceneIdForNPC helper** (add before the `onInteractNPC` event listener setup):
```typescript
const getSceneIdForNPC = useCallback((npcId: string, baseSceneId: string): string => {
  const d = decisionsRef.current;

  // Security Analyst Sam — fax incident handling
  if (npcId === 'security_analyst') {
    if (d.faxIncidentHandled === 'reported') {
      return 'security_analyst_variant_fax_reported';
    }
    if (d.faxIncidentHandled === 'delayed' || d.faxIncidentHandled === 'ignored') {
      return 'security_analyst_variant_fax_delayed_ignored';
    }
    // Vendor credential sharing (check secondary decision)
    if (d.vendorAccessGranted === true) {
      return 'security_analyst_variant_vendor_credentials';
    }
  }

  // Records Clerk — journey acknowledgment (Act 2+)
  // Read roomData.json to confirm records clerk npcId before hardcoding here
  // Common IDs to check: 'records_clerk', 'records_clerk_joanna', 'joanna'
  const RECORDS_CLERK_NPC_ID = 'records_clerk'; // Update to match roomData.json
  if (npcId === RECORDS_CLERK_NPC_ID && actState.current >= 2) {
    // Only show journey variant once — check if they've seen the base scene already
    // If completedNPCs includes this NPC, they've seen their base scene; show the variant
    if (completedNPCs.has(npcId)) {
      return 'records_clerk_variant_journey';
    }
  }

  return baseSceneId;
}, []); // decisionsRef and actState are refs/stable state — no deps needed
```

Read roomData.json to find the exact NPC ID for the records clerk. Update `RECORDS_CLERK_NPC_ID` to match.

**Step 3 — Wire getSceneIdForNPC into onInteractNPC:**
Find the existing `onInteractNPC` handler (inside the EventBridge `useEffect`). Update the handler to call `getSceneIdForNPC`:

Change:
```typescript
const onInteractNPC = (data: { npcId: string; npcName: string; sceneId: string }) => {
  setCurrentNPCId(data.npcId);
  setCurrentSceneId(data.sceneId);
  setPageMode('dialogue');
};
```

To:
```typescript
const onInteractNPC = (data: { npcId: string; npcName: string; sceneId: string }) => {
  setCurrentNPCId(data.npcId);
  const resolvedSceneId = getSceneIdForNPC(data.npcId, data.sceneId);
  setCurrentSceneId(resolvedSceneId);
  setPageMode('dialogue');
};
```

**Important:** The `onInteractNPC` handler is defined inside a `useEffect` (see PrivacyQuestPage.tsx around line 385). `getSceneIdForNPC` uses refs internally so it does not need to be in the `useEffect` deps array — but the call to `getSceneIdForNPC` must be accessible. If `getSceneIdForNPC` is defined outside the `useEffect` (in component scope), it is always accessible.

Verify that `actState` is accessible from `getSceneIdForNPC`. Since `actState` comes from `useGameState()` at component scope, it is available. However for safe ref access, add:
```typescript
const actStateRef = useRef(actState);
useEffect(() => { actStateRef.current = actState; }, [actState]);
```
And use `actStateRef.current.current` (the act number) inside `getSceneIdForNPC`.
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>getSceneIdForNPC is defined and called in onInteractNPC. TypeScript compiles clean. The security_analyst NPC routes to variant scenes based on decision flags. No existing NPC routing is broken.</done>
</task>

</tasks>

<verification>
Manual smoke test (after npm run dev):

**Test A — No flags set (default routing):**
1. Start fresh game (clear localStorage). Interact with Security Analyst Sam. Should see the original base scene, not a variant.

**Test B — Fax reported variant:**
1. In Break Room, interact with Fax Machine Freddy NPC. Choose "Document the incident and notify your Privacy Officer immediately" (score +20).
2. Navigate to IT Office. Interact with Security Analyst Sam. Should see: "Word travels fast in compliance. I heard about the fax incident — you reported it right away..."

**Test C — Fax ignored variant:**
1. Clear localStorage. In Break Room, choose "Don't tell anyone" (score -20).
2. In IT Office, interact with Sam. Should see: "Look, I'm going to be straight with you. That fax situation a while back..."

**Test D — Records Clerk journey variant:**
1. Complete Act 1 (Reception + Break Room). Enter Records Room after act advances to 2.
2. Interact with Records Clerk. On second visit (after first NPC completion), should see: "You know, I've seen a lot of new employees..."

TypeScript: `npx tsc --noEmit` passes.
</verification>

<success_criteria>
At least 2 NPCs reference a specific earlier player decision — the dialogue text reflects what the player actually chose, not a generic line. Default dialogue is unchanged when no flags are set. All variant lines sound like a person talking, not a compliance document.
</success_criteria>

<output>
After completion, create `.planning/phases/14-three-act-narrative/14-04-SUMMARY.md`
</output>
