---
phase: 26-room-visual-uplevel
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - client/src/phaser/SpriteFactory.ts
  - client/src/phaser/scenes/ExplorationScene.ts
autonomous: true
requirements: [VIS-05, VIS-06]
must_haves:
  truths:
    - "Hallway benches and wall sconces render as benches and sconces — not as fallback wooden desks (16 obstacle instances fixed)"
    - "The 8 most-seen furniture textures (plant, chair, filing_cabinet, cable_tray, server_rack, desk, table, vending_machine) have 1px silhouette outlines and stronger shading — readable at 32px"
    - "Uncollected educational items show a soft pulsing filled glow aura plus periodic sparkle particles — distinguishable from furniture at a glance"
    - "Plants sway in every room they appear (14 instances), screens flicker on server_rack/monitor_bank/vital_monitor, and the break room coffee station steams — type-driven, not room-hardcoded"
    - "No gameplay or collision changes; tsc and production build clean"
  artifacts:
    - path: "client/src/phaser/SpriteFactory.ts"
      provides: "Detail-passed 8 generators + new furn_wall_sconce and furn_bench generators + furnitureTextureKey map entries"
      contains: "furn_wall_sconce"
    - path: "client/src/phaser/scenes/ExplorationScene.ts"
      provides: "Collectible glow/sparkle up-level + type-driven furniture idle animation pass"
      contains: "addFurnitureIdleAnimations"
  key_links:
    - from: "client/src/phaser/SpriteFactory.ts furnitureTextureKey"
      to: "roomData.json obstacle types wall_sconce/bench"
      via: "map entries wall_sconce: 'furn_wall_sconce', bench: 'furn_bench'"
      pattern: "wall_sconce.*furn_wall_sconce"
    - from: "ExplorationScene idle animation pass"
      to: "room.obstacles by type"
      via: "type-driven iteration (plant / server_rack / monitor_bank / vital_monitor / coffee_station)"
      pattern: "monitor_bank|vital_monitor"
    - from: "ExplorationScene educational item render"
      to: "particle_circle texture"
      via: "sparkle particle emitter on uncollected items"
      pattern: "particle_circle"
---

<objective>
Upgrade the most-seen furniture: detail pass on the 8 highest-frequency textures, two new generators (wall sconce, bench) that kill the worst fallback offenders in the hallways, an unmissable glow + sparkle treatment on educational collectibles, and a type-driven idle-animation pass so plants sway, screens flicker, and coffee steams in every room they appear.

Purpose: Phase 26 success criteria 2 and 3 — furniture detail pass + collectible glow (Commandment 9), and at least 3 furniture types with subtle idle motion so rooms feel alive.
Output: Reworked SpriteFactory generators + map; ExplorationScene collectible glow and idle-animation passes.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/phaser/SpriteFactory.ts
@client/src/phaser/scenes/ExplorationScene.ts

Investigation findings (verified 2026-06-10, do not re-derive):
- **Furniture frequency from roomData.json obstacle counts** (the "8 most-seen" is settled — do not re-derive): plant 14, chairs 9 (+3 decorative reception chairs), wall_sconce 9, filing_cabinet 8, bench 7, cable_tray 6, server_rack 5, desk 4, table 4, vending_machine 3. `wall_sconce` and `bench` (all in the 5 hallway rooms) have NO entry in `furnitureTextureKey` and fall back to `'furn_desk'` — 16 wooden desks rendering where sconces and benches belong. Other unmapped one-offs also falling back to furn_desk: biohazard_sign, glove_dispenser, filing_cart, emergency_light, defibrillator_mount, wheelchair (1 each).
- SpriteFactory.ts generator pattern: `if (!scene.textures.exists('furn_X')) { const g = scene.add.graphics(); ...fillRect pixel art on 32x32 canvas...; g.generateTexture('furn_X', TILE, TILE); g.destroy(); }`. Helpers `darken(color, amt)` / `lighten(color, amt)` at top of file. Generators: furn_desk ~line 453, furn_table ~601, furn_chair ~764, furn_plant ~813, furn_filing_cabinet ~926, furn_vending_machine ~1984, furn_server_rack ~2139, furn_cable_tray ~3358. `furnitureTextureKey` map at ~line 3669. All textures generated once from BootScene via `generateAllTextures`.
- ExplorationScene furniture render: obstacles loop ~lines 722-744 (1x1 sprite + drop shadow; multi-tile tiles the sprite per cell at depth 3). Furniture sprites are NOT stored in any array — idle-anim pass must iterate `room.obstacles` and compute positions from tile coords (precedent: entrance plant-sway at lines ~1165-1172 does exactly this).
- Educational item render: ~lines 773-820. Uncollected items already get: bob tween (y-4, 600ms yoyo), alpha shimmer (1→0.7), and a WEAK glow — `add.circle(x, y, 14, 0xffd700, 0)` with stroke-only pulsing to alpha 0.5. This stroke ring is the part to up-level; keep the bob, replace the shimmer + ring.
- Existing idle-animation precedents (keep, but dedupe where the new pass covers them): entrance coffee-cart steam puffs (~1156-1159), entrance-only plant leaf-sway (~1165-1172), break room vending LED cycle (~1186-1191), records fluorescent flicker (~1219+), reception mug bob (~1110). The entrance plant-sway block must be REMOVED once the generic pass covers plants everywhere (no double tweens on entrance plants).
- `particle_circle` texture: generated in BootScene (~line 401-405), 6x6 white circle; ExplorationScene already guards usage with `this.textures.exists('particle_circle')`.
- Type-driven targets by room: plant in 9 rooms (entrance, all 5 hallways, reception, it_office, +); server_rack in it_office + hallway_records_it; monitor_bank in it_office; vital_monitor in er; coffee_station in break_room (1 obstacle) plus a decorative coffee-cart sprite in entrance that already steams.
- Scene restarts per room transition — tweens/timers registered via `this.tweens.add` / `this.time.addEvent` are cleaned up by Phaser on scene shutdown; existing code relies on this, follow the same pattern.
- Verification commands: `npm run check` (tsc) and `npm run build`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpriteFactory detail pass + furn_wall_sconce + furn_bench + map fixes</name>
  <files>client/src/phaser/SpriteFactory.ts</files>
  <action>
**A. Detail pass on the 8 most-seen generators** (furn_plant, furn_chair, furn_filing_cabinet, furn_cable_tray, furn_server_rack, furn_desk, furn_table, furn_vending_machine). For each, keep the existing color identity and silhouette footprint (so nothing shifts on the grid) and add:
1. A 1px dark silhouette outline — draw the outline color (darken(baseColor, 60) or 0x1a1a1a where the sprite is multi-colored) as edge rects around the sprite's outer silhouette. Simplest reliable approach per generator: before the existing body fills, draw the silhouette shape 1px larger in outline color; the existing fills then leave a 1px rim. Readability at 32px is the goal — the sprite should pop off any floor.
2. Boosted contrast: bump existing highlight fills with lighten(+10 more) and shadow fills with darken(+10 more) where the generator already places them.
3. One or two characterful details each (stay inside the 32px canvas, SNES-style): plant — second leaf-cluster tone + 2px pot rim shine; chair — 1px stitch line across the seat cushion; filing_cabinet — small white label slots on each drawer + one drawer drawn 1px ajar with inner shadow; cable_tray — 3 colored cable runs (muted red/blue/yellow 2px lines) inside the tray; server_rack — vent slat lines + 2 LED dots baked in (one green 0x44ff44, one amber); desk — a paper sheet (white 6x4 rect, 1px grey edge) and a 2px pen on the desktop; table — wood grain (2 thin darker streaks) + corner wear highlight; vending_machine — 3 product-silhouette rows (small colored 3x4 rects behind the window) + coin-slot detail.

**B. Two new generators** appended in generateFurnitureTextures, same `if (!exists)` pattern:
- `furn_wall_sconce`: wall-mounted light — dark metal bracket (0x4a4a52) at top-center, upward trapezoid lamp shade (0xc8b88a), warm glow blob above it (0xffe8a8 at descending alphas 0.5/0.25 in 2 expanding rects), occupying roughly x 10-22, y 4-22 (upper 2/3 of tile, since it represents a wall fixture). 1px outline like the others.
- `furn_bench`: corridor bench — horizontal wooden seat (0x8b6f47 family, 26px wide, ~6px tall at y≈14) with 3 slat lines, two darker legs, and a low backrest bar (2px) above the seat. Reuse the desk's wood palette helpers. 1px outline.

**C. `furnitureTextureKey` map fixes** (~line 3669) — add entries:
`wall_sconce: 'furn_wall_sconce'`, `bench: 'furn_bench'`, and nearest-credible remaps for the one-offs: `biohazard_sign: 'furn_notice_board'`, `glove_dispenser: 'furn_hand_sanitizer'`, `filing_cart: 'furn_document_cart'`, `emergency_light: 'furn_wall_sconce'`, `defibrillator_mount: 'furn_crash_cart'`, `wheelchair: 'furn_chair'`. The `|| 'furn_desk'` fallback stays for genuinely unknown types.
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && grep -q "wall_sconce: 'furn_wall_sconce'" client/src/phaser/SpriteFactory.ts && grep -q "bench: 'furn_bench'" client/src/phaser/SpriteFactory.ts</automated>
  </verify>
  <done>8 generators carry outline + contrast + character details; furn_wall_sconce and furn_bench generators exist; all 8 previously-fallback obstacle types map to credible textures; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Educational collectible glow + sparkle up-level</name>
  <files>client/src/phaser/scenes/ExplorationScene.ts</files>
  <action>
In the educational items block (~lines 773-820), for UNCOLLECTED items only, replace the weak stroke-ring + alpha-shimmer treatment with an at-a-glance glow (keep the existing bob tween and the collected-state dimming exactly as-is):

1. **Filled glow aura**: behind the item sprite (depth `sprite.depth - 1`), add a soft radial glow using the existing `particle_circle` texture as an image — `this.add.image(sprite.x, sprite.y + 2, 'particle_circle')` scaled to ~7 (≈42px), tinted gold 0xffd700, `setBlendMode(Phaser.BlendModes.ADD)`, base alpha 0.18. Tween it: alpha 0.18→0.38 and scale 6→8, 1100ms, yoyo, repeat -1, Sine.easeInOut. Guard with `this.textures.exists('particle_circle')`; if missing, fall back to an `add.circle` with filled gold alpha pulse (never stroke-only).
2. **Periodic sparkle**: a small particle emitter on the item — `this.add.particles(sprite.x, sprite.y, 'particle_circle', { speed: { min: 8, max: 20 }, scale: { start: 0.8, end: 0 }, alpha: { start: 0.9, end: 0 }, tint: [0xffffff, 0xffe9a0], lifespan: 500, frequency: 1400, quantity: 2, angle: { min: 0, max: 360 } })` at depth `sprite.depth + 1`. This is the Zelda "this thing matters" twinkle (Commandment 9).
3. **Remove** the old alpha-shimmer tween (alpha 1→0.7) — it fights the new glow and makes the item itself look faded — and remove the old stroke-ring circle + its tween entirely. The item sprite stays full alpha; the AURA pulses instead. Note: the item bob tween moves the sprite, the glow stays anchored — that's fine (glow reads as floor-glow under a hovering item); do not parent them.

Collected items: unchanged (0.4 alpha, grey tint, no glow, no sparkle).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && ! grep -q "strokeAlpha: { from: 0, to: 0.5 }" client/src/phaser/scenes/ExplorationScene.ts</automated>
  </verify>
  <done>Uncollected items render a pulsing additive gold aura + periodic sparkles; old stroke ring and item-alpha shimmer removed; collected-state rendering untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Type-driven furniture idle animations + dedupe + regression gate</name>
  <files>client/src/phaser/scenes/ExplorationScene.ts</files>
  <action>
**A.** Add a private method `addFurnitureIdleAnimations(room: RoomData-equivalent)` called once from `create()` after the obstacles render loop. It iterates `room.obstacles` and attaches overlay animations by type (positions computed from tile coords like the existing entrance plant-sway precedent; all via `this.tweens.add` / `this.time.addEvent` so scene shutdown cleans them up):

1. **`plant` — leaf sway** (14 instances across 9 rooms): port the existing entrance-only implementation — translucent green ellipse (14x6, 0x4a8a3a, alpha 0.45, depth 4) at the plant's crown (`obs.x * TILE + (obs.width * TILE) / 2`, `obs.y * TILE + 4`), angle tween -6→6, duration 1900 + Math.random() * 400, yoyo, repeat -1.
2. **`server_rack` / `monitor_bank` / `vital_monitor` — screen flicker + LED blink**: per obstacle, (a) a small screen-glow rect (10x6, 0x66ccff for monitor_bank/vital_monitor, 0x44ff66 for server_rack, alpha 0.30, depth 20) positioned over the sprite's upper half, with a looping timer (`this.time.addEvent`, delay 2400 + Math.random() * 1800) that briefly drops its alpha to 0.08 for ~120ms then restores (two chained tweens or tween with yoyo+hold) — a CRT flicker, not a strobe; (b) a 2x2 LED dot that toggles between 0x44ff44 and 0x227722 every ~900ms via a timer.
3. **`coffee_station` — steam puffs**: port the entrance coffee-cart steam (ellipse puff, rises 12px, fades, scales 1.5x, 1400ms, spawned every ~2600ms) keyed off the obstacle position. Used by break_room's coffee_station.

Use random per-instance phase offsets (duration/delay jitter already specified) so animations don't sync across instances.

**B. Dedupe**: remove the now-redundant entrance-only plant leaf-sway block inside the `hospital_entrance` branch (~lines 1165-1172) — the generic pass covers it. KEEP the entrance coffee-cart steam (it's a decorative sprite, not a roomData obstacle, so the generic pass won't reach it), the break room vending LED, the records fluorescent flicker, and the reception mug bob.

**C. Regression gate**:
- `npm run check` and `npm run build` both clean.
- `git diff` shows zero changes to: collision-body code, door logic, interaction/BFS logic, roomData.json, and the educational item INTERACTION path (only its visuals changed in Task 2).
- Confirm at least 3 furniture types animate by grep: plant sway, screen flicker (server_rack/monitor_bank/vital_monitor), coffee steam all present in `addFurnitureIdleAnimations`.
- Optional (do not block on failure): if a dev server starts cleanly within budget, capture a screenshot via existing `tests/visual-qa.spec.ts` for the SUMMARY; otherwise note that live look-check is deferred to the user (established Phase 23-25 pattern).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && npm run build && grep -q "addFurnitureIdleAnimations" client/src/phaser/scenes/ExplorationScene.ts</automated>
  </verify>
  <done>addFurnitureIdleAnimations covers plant/screens/coffee types in all rooms; entrance plant-sway dedupe done; tsc + production build clean; no gameplay/collision diffs.</done>
</task>

</tasks>

<verification>
- `npm run check` and `npm run build` pass.
- `grep "furn_wall_sconce\|furn_bench" client/src/phaser/SpriteFactory.ts` shows both generators and both map entries — no hallway bench/sconce falls back to furn_desk.
- `grep -A2 "particle_circle" client/src/phaser/scenes/ExplorationScene.ts` shows the collectible aura + sparkle emitter; old stroke-ring code gone.
- `addFurnitureIdleAnimations` exists, is called from create(), and handles ≥3 furniture types (plant, screen-bearing, coffee_station).
- `git diff` clean of collision, door, interaction, and roomData.json changes.
</verification>

<success_criteria>
- The 8 most-seen furniture textures pop at 32px (outline + contrast + character detail); 16 hallway fallback-desks replaced with real sconce/bench art (VIS-05).
- Educational collectibles are identifiable at a glance via pulsing additive glow + sparkles (VIS-05, Commandment 9).
- Plants sway everywhere, IT/ER screens flicker, break room coffee steams — type-driven idle motion on ≥3 furniture types (VIS-06).
- Zero gameplay or collision changes; live look-check deferred to user (Phase 23-25 precedent).
</success_criteria>

<output>
After completion, create `.planning/phases/26-room-visual-uplevel/26-02-SUMMARY.md`
</output>
