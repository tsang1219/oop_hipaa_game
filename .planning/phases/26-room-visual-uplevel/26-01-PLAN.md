---
phase: 26-room-visual-uplevel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - client/src/phaser/scenes/ExplorationScene.ts
autonomous: true
requirements: [VIS-04]
must_haves:
  truths:
    - "Reception renders a floor treatment visibly distinct from the hallway default — it no longer shares the generic beige grid"
    - "Hallways render a runner strip down the middle walkway row — they read as corridors, not generic rooms"
    - "Floor tiles directly beneath wall bottoms show a contact-shadow gradient — walls visibly meet floors instead of floating on the grid"
    - "Each of the 6 departments + entrance keeps/gains a distinct palette AND pattern (ER pale-clinical, Lab green clean-room, IT dark raised panels, Break Room wood planks, Records carpet, Entrance marble, Reception new treatment)"
    - "VIS-04, VIS-05, VIS-06 are defined in REQUIREMENTS.md with traceability rows"
    - "No collision or gameplay change — floor work is render-only; tsc and production build clean"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Room Visual Up-Level (Phase 26) section defining VIS-04..06"
      contains: "VIS-04"
    - path: "client/src/phaser/scenes/ExplorationScene.ts"
      provides: "FLOOR_STYLES per-room-type floor config + reception branch + hallway runner + wall contact shadow"
      contains: "FLOOR_STYLES"
  key_links:
    - from: "client/src/phaser/scenes/ExplorationScene.ts floor render loop"
      to: "room.obstacles wall entries"
      via: "contact-shadow pass over floor tiles adjacent to wall bottoms"
      pattern: "obs.y \\+ obs.height|contactShadow|wall.*shadow"
    - from: "FLOOR_STYLES"
      to: "roomId branch selection"
      via: "roomId.includes matching incl. explicit 'hallway' branch"
      pattern: "hallway"
---

<objective>
Define the VIS-04..06 requirements (marked TBD in REQUIREMENTS.md) and up-level the per-room floor system in ExplorationScene: reception gets its own treatment instead of sharing the hallway default, hallways get a corridor runner strip, every department's pattern gets a distinctness boost, and a wall/floor contact shadow makes walls visibly sit ON the floor.

Purpose: Phase 26 success criterion 1 — "Each department has a visually distinct floor treatment (palette + pattern variation per room type) with visible wall/floor transition — rooms no longer look like recolors of the same grid."
Output: REQUIREMENTS.md v2.3 Phase 26 section; reworked floor rendering block in ExplorationScene.ts.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/phaser/scenes/ExplorationScene.ts

Investigation findings (verified 2026-06-10, do not re-derive):
- Floor rendering lives in ExplorationScene.ts `create()` at ~lines 305-449: a single `this.add.graphics()` pass drawn once per room load (NOT per frame). Per-tile: base shade from a 4-color `tileShades` array, 1px bevel highlight/shadow edges, linoleum cross pattern on alternating tiles, hashed specular gleam, grout lines, then a `roomId.includes(...)` chain adding room-specific pattern details (ER diamond, Lab grid dots + drain lines, Break Room parquet planks, IT raised-panel vents, Records carpet dots, Entrance marble veining).
- Palette selection is an if/else chain on `roomId = room.id.toLowerCase()` at lines 311-346. Branches exist for er/emergency, lab, it/server, break, records, entrance/lobby. **Reception and all 5 hallways fall into the same default beige branch** — this is the "recolors of the same grid" offender.
- Room ids (12): hospital_entrance, hallway_reception_break, hallway_break_lab, hallway_lab_records, hallway_records_it, hallway_it_er, reception, er, lab, records_room, it_office, break_room. Hallways are 20x7 tiles (middle walkway row y=3); reception is 20x15.
- Room ambient tint overlay at lines 452-466 (`roomTints` record) — keep as-is.
- Walls render at lines 652-721 (per-tile brick pattern with room-specific colors); they already have a bottom shadow ON THE WALL TILE (lines 698-700) but the FLOOR tile below the wall has no corresponding contact shadow — walls look pasted on top of the floor grid.
- Baseboard strip is drawn by `decorGfx` at lines 471-484 along the bottom of wall obstacles.
- Wall collision bodies are built separately at lines 746-770 — do NOT touch anything in that block.
- TILE = 32. Hash pattern for deterministic per-tile variation already in use: `((x * 7 + y * 13 + 37) * 2654435761) >>> 0`.
- REQUIREMENTS.md: v2.3 section at ~line 139 says "VIS-04..08 are reserved for Phases 26/27 to define"; VIS-01..03 entries (~lines 143-147) are the format precedent. Traceability table has VIS-01..03 rows at ~lines 248-250; coverage line "v2.3 requirements: 3 total" at ~line 256.
- Verification commands: `npm run check` (tsc) and `npm run build` (vite + esbuild).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define VIS-04..06 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add a `### Room Visual Up-Level (Phase 26)` subsection to the v2.3 Requirements section (after the VIS-03 entry), following the VIS-01..03 checkbox format. Update the intro note so VIS-04..06 are no longer described as reserved (VIS-07..08 stay reserved for Phase 27). Define:

- **VIS-04**: Each department and connector renders a visually distinct floor treatment — distinct palette + pattern per room type (ER pale clinical tile with safety accents, Lab green clean-room grid, IT dark raised panels, Break Room wood planks, Records carpet, Entrance marble, Reception its own treatment distinct from the default, hallways a corridor runner strip down the walkway row). Floor tiles adjacent to wall bottoms render a contact-shadow gradient so walls visibly meet floors. Floor rendering stays a once-per-room-load Graphics pass (no per-frame draw); zero collision changes.
- **VIS-05**: The 8 most-frequently-rendered furniture textures (furn_plant, furn_chair, furn_filing_cabinet, furn_cable_tray, furn_server_rack, furn_desk, furn_table, furn_vending_machine — derived from roomData.json obstacle frequency counts) get a detail pass: 1px dark silhouette outline, boosted highlight/shadow contrast, 1-2 characterful details each, readable at 32px. The 16 hallway `wall_sconce` (9) and `bench` (7) obstacles stop falling back to the desk texture via two new generators (furn_wall_sconce, furn_bench) plus map entries; remaining unmapped one-off types get nearest-credible remaps. Uncollected educational collectibles display an at-a-glance glow: soft pulsing filled aura behind the item plus periodic sparkle particles, additive to the existing bob (Commandment 9).
- **VIS-06**: At least 3 furniture types display a subtle, type-driven idle animation in every room where they appear (not just one hardcoded room): plant leaf-sway (all 14 plants), screen flicker + LED blink on server_rack / monitor_bank / vital_monitor, coffee steam on coffee_station. Animations are created once per room load via tweens/timers; no gameplay or collision changes.

Add three traceability rows (`VIS-04 | Phase 26 | Pending` etc.) after the VIS-03 row, and update the v2.3 coverage count from 3 to 6 (mapped: 6, unmapped: 0).
  </action>
  <verify>
    <automated>grep -c "VIS-04\|VIS-05\|VIS-06" .planning/REQUIREMENTS.md | awk '{exit !($1 >= 6)}'</automated>
  </verify>
  <done>VIS-04..06 defined in the v2.3 section with checkbox entries, traceability rows present, coverage count updated to 6.</done>
</task>

<task type="auto">
  <name>Task 2: Per-room floor distinctness — FLOOR_STYLES extraction, reception treatment, hallway runner</name>
  <files>client/src/phaser/scenes/ExplorationScene.ts</files>
  <action>
Rework the floor palette/pattern selection (lines ~305-449) without changing how/when the Graphics object is created (still one pass per room load, same depth ordering):

1. **Extract a module-level `FLOOR_STYLES` config** (near the top of the file, after TILE const): a record keyed by style name (`er`, `lab`, `it`, `break`, `records`, `entrance`, `reception`, `hallway`, `default`) each holding `{ tileShades: number[], highlightColor: number, shadowColor: number }`. Move the existing six branch palettes in verbatim. Add a small `floorStyleFor(roomId: string): keyof typeof FLOOR_STYLES` helper using the existing `roomId.includes(...)` checks PLUS two new checks: `roomId.includes('hallway')` → 'hallway' and `roomId.includes('reception')` → 'reception'. Order matters: check 'hallway' BEFORE 'reception'/'break' etc. since hallway ids contain department names (e.g. `hallway_reception_break` must resolve to 'hallway', not 'reception').

2. **Reception treatment** (new, must read differently from both ER white-blue and the beige default): warm cream porcelain — tileShades around [0xe2d8c4, 0xded4c0, 0xdad0bc, 0xe6dcc8], highlight 0xf2e8d4, shadow 0xbeb49e — with TWO pattern signatures in the per-tile loop: (a) large-format tile illusion — draw grout lines only on every 2nd tile boundary (when `x % 2 === 1` draw right grout, when `y % 2 === 1` draw bottom grout) instead of every tile, so reception reads as 64px porcelain slabs; (b) a small navy accent diamond (0x2c4a6e at ~0.18 alpha, four fillRects forming a 6px diamond) at each 2x2 slab intersection (`x % 2 === 1 && y % 2 === 1`, drawn at the tile's bottom-right corner).

3. **Hallway treatment**: keep a beige-adjacent base (slightly cooler/dimmer than old default, e.g. shades [0xccc2a6, 0xc8bea2, 0xc4ba9e, 0xd0c6aa]) and add a **runner strip** along the walkway: for hallway rooms, after the per-tile loop, draw a horizontal runner across row y=3 (the 20x7 walkway row) inset 1 tile from each end — a muted teal base (0x3e6b6b, alpha 0.55) spanning `TILE` height with: 2px darker border lines (0x2a4a4a, alpha 0.7) along top and bottom edges, and short stitch ticks (3px, 0x5a8a8a, alpha 0.5) every 16px along both borders. Draw it into the same floor Graphics so depth is unchanged.

4. **Distinctness audit of existing branches** — keep all six existing room patterns but boost the two weakest so each room passes a "screenshot without furniture is identifiable" bar: Records carpet — add row-offset weave (shift the existing dot grid 4px on odd rows, and add faint horizontal pile lines 0x000000 at 0.04 every 8px); ER — add a pale red accent border tile ring adjacent to the room's door tiles only (use `(room as any).doors` array: for floor tiles within 1 tile of a door, overlay 0xff6b6b at 0.07) for the "urgent pale tile with hazard accents near doors" read.

Do not modify: the `roomTints` overlay, ceiling light pools, ambient particles, the wall render block, or anything below line ~466. Do not touch collision-body code (lines ~746-770).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check</automated>
  </verify>
  <done>FLOOR_STYLES module-level config exists; reception and hallway have dedicated branches with the patterns above; hallway ids resolve to 'hallway' (not a department style); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wall/floor contact shadow + build verification</name>
  <files>client/src/phaser/scenes/ExplorationScene.ts</files>
  <action>
Add a contact-shadow pass so walls visibly sit ON the floor (the classic SNES ambient-occlusion strip):

After the per-tile floor loop (still drawing into the same `floor` Graphics object, before the roomTints overlay), iterate `room.obstacles` entries with `type === 'wall'`. For each wall, for every tile column `wx` in `[obs.x, obs.x + obs.width)`, the floor tile at row `obs.y + obs.height` (directly below the wall bottom) gets a 3-step vertical gradient at its top edge: fillRect(wx*TILE, rowY*TILE, TILE, 3) at 0x000000 alpha 0.18, then rows +3..+5 at alpha 0.10, then +5..+8 at alpha 0.05. Skip tiles that are outside room bounds (`rowY >= room.height`) and tiles occupied by another obstacle (check `room.obstacles.some(...)` covering that tile) so shadows never paint on top of furniture footprints or other walls. Also skip door tiles (`(room as any).doors` within 1 tile horizontally) so doorway floors stay clean and walkable-looking.

Then run full verification:
- `npm run check` — tsc clean.
- `npm run build` — production build clean.
- Confirm render-only: `git diff client/src/phaser/scenes/ExplorationScene.ts` must show no changes inside the collision-body block (the `this.walls` staticGroup / `add.rectangle` invisible bodies section) and no changes to `roomData.json`.
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run check && npm run build</automated>
  </verify>
  <done>Floor tiles below wall bottoms render a 3-step contact shadow with obstacle/door exclusions; tsc and production build clean; zero collision-code diff.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (tsc clean).
- `npm run build` passes (vite + esbuild production build).
- `grep -n "FLOOR_STYLES" client/src/phaser/scenes/ExplorationScene.ts` shows the module-level config and its use in create().
- `grep -n "hallway" client/src/phaser/scenes/ExplorationScene.ts` shows the hallway style branch resolves before department-name matches.
- REQUIREMENTS.md contains VIS-04, VIS-05, VIS-06 definitions + traceability rows; v2.3 coverage says 6.
- `git diff` confirms no changes to collision bodies, roomData.json, or gameplay logic.
</verification>

<success_criteria>
- Reception, hallways, and the 6 departments + entrance each resolve to a distinct FLOOR_STYLES entry with a distinct pattern — no two room types share both palette and pattern.
- Hallways show a bordered runner strip on the walkway row; reception shows 64px porcelain slabs with navy accent diamonds.
- Wall bottoms cast a visible gradient contact shadow onto adjacent floor tiles, skipping doors and furniture footprints.
- VIS-04..06 exist in REQUIREMENTS.md; VIS-04 is fully implemented by this plan.
- Live look-check deferred to user per established pattern (Phases 23-25 precedent) — no human-verify checkpoint blocks execution.
</success_criteria>

<output>
After completion, create `.planning/phases/26-room-visual-uplevel/26-01-SUMMARY.md`
</output>
