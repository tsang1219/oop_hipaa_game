---
phase: 24-phi-sorter-format-shift
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/phi-sorter/DeskSurface.tsx
  - client/src/components/phi-sorter/ShiftClock.tsx
  - client/src/components/phi-sorter/OutgoingTray.tsx
  - client/src/components/phi-sorter/StampPad.tsx
  - client/src/components/phi-sorter/DeskDocument.tsx
  - client/src/components/phi-sorter/NPCReactionBubble.tsx
autonomous: true
requirements: [SORTV2-12, SORTV2-13, SORTV2-14, SORTV2-15]

must_haves:
  truths:
    - "Five new desk-format components exist with the exact prop contracts specified, compile clean, and are purely presentational (no EventBridge, no timers that outlive unmount, no game-state knowledge)"
    - "DeskDocument renders all Phase 22 chart fields (patientName/age/role/reasonForVisit/emergencyContact/doctorNote/miscField) on a paper-styled surface and overlays a rotated ink mark + ink-splatter burst when stamped"
    - "StampPad renders KEEP (green) and REDACT (red) stamps that support mouse click AND a keyboard-focus visual state driven by props"
    - "NPCReactionBubble accepts an optional spriteUrl prop and renders a persistent 64px pixelated portrait + name plate; with no spriteUrl it behaves exactly as before (backward compatible)"
  artifacts:
    - path: "client/src/components/phi-sorter/DeskDocument.tsx"
      provides: "One-at-a-time paper document with animState-driven slide-in/stamped/slide-off + ink mark"
      exports: ["DeskDocument"]
      min_lines: 80
    - path: "client/src/components/phi-sorter/StampPad.tsx"
      provides: "KEEP/REDACT stamp buttons with focus + press states"
      exports: ["StampPad"]
    - path: "client/src/components/phi-sorter/OutgoingTray.tsx"
      provides: "Stacked-paper tray visual with bouncing count"
      exports: ["OutgoingTray"]
    - path: "client/src/components/phi-sorter/ShiftClock.tsx"
      provides: "mm:ss countdown display with low-time pulse"
      exports: ["ShiftClock"]
    - path: "client/src/components/phi-sorter/DeskSurface.tsx"
      provides: "Wood-grain CSS desk container"
      exports: ["DeskSurface"]
  key_links:
    - from: "client/src/components/phi-sorter/DeskDocument.tsx"
      to: "client/src/data/sorterData.ts"
      via: "SorterItem type import — chart fields render"
      pattern: "from '@/data/sorterData'"
    - from: "client/src/components/phi-sorter/DeskDocument.tsx"
      to: "client/src/index.css"
      via: "animate-[doc-slide-in...] / doc-slide-off / ink-stamp-in classes"
      pattern: "doc-slide-in"
---

<objective>
Build the five presentational desk-format components (DeskSurface, ShiftClock, OutgoingTray, StampPad, DeskDocument) and extend NPCReactionBubble with a persistent portrait — everything Plan 03's overlay rewrite will compose. All components are self-contained, prop-driven, and match the pixel-art Press Start 2P aesthetic.

Purpose: interface-first — Plan 03 receives finished, typed building blocks and only does composition + state machine work.
Output: 5 new components + 1 extended component, all compiling, none yet mounted (PHISorterOverlay untouched until Plan 03).
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/components/phi-sorter/SorterItem.tsx (chart-field rendering being ported to DeskDocument — ChartLine pattern)
@client/src/components/phi-sorter/BucketZone.tsx (particle burst + counter-bounce patterns being ported to DeskDocument/OutgoingTray)
@client/src/components/phi-sorter/NPCReactionBubble.tsx (extended in Task 3)
@client/src/components/CertificateOverlay.tsx (lines 235-250 — CSS spritesheet-crop portrait pattern from Phase 21)
@client/src/data/sorterData.ts (SorterItem + SorterChart types)

<interfaces>
<!-- Existing contracts the executor needs — no exploration required. -->

From client/src/data/sorterData.ts:
```typescript
export type SorterChart = {
  patientName: string;
  age?: number;
  role?: string;
  emergencyContact?: string;
  reasonForVisit?: string;
  doctorNote?: string;
  miscField?: { label: string; value: string };
};
export type SorterItem = {
  id: string;
  label: string;
  category: 'phi' | 'not_phi';
  identifierType?: SorterIdentifierType;
  explanation: string;
  chart: SorterChart;
  holdIt?: SorterHoldIt;
};
```

From client/src/components/CertificateOverlay.tsx (Phase 21 portrait pattern — spritesheets are 96×128px, 3×4 grid of 32×32 frames; frame 0 = idle-down at top-left):
```typescript
// 64px portrait = 2× upscale: backgroundSize '192px 256px', backgroundPosition '0 0',
// width/height 64px, backgroundRepeat 'no-repeat', imageRendering: 'pixelated'
```

CSS keyframes contract (defined by Plan 24-01, consumed via Tailwind arbitrary values — runtime-only coupling, safe to build in parallel):
doc-slide-in (300ms), doc-slide-off-left (300ms), doc-slide-off-right (300ms),
ink-stamp-in (150ms, uses --ink-rot CSS var), stamp-press (150ms), clock-pulse (600ms loop),
plus existing: sorter-particle, counter-bounce, flash-green, shake-red
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeskSurface + ShiftClock + OutgoingTray (simple presentational trio)</name>
  <files>client/src/components/phi-sorter/DeskSurface.tsx, client/src/components/phi-sorter/ShiftClock.tsx, client/src/components/phi-sorter/OutgoingTray.tsx</files>
  <action>
All three use Press Start 2P inline font style (match existing phi-sorter components) and `data-testid` attributes as specified.

**DeskSurface.tsx** — `export function DeskSurface({ children }: { children: React.ReactNode })`. A wood-desk container div (`data-testid="sorter-desk-surface"`): CSS wood texture via layered gradients, NO image assets — e.g. base `#6B4A2F` with `repeating-linear-gradient(90deg, rgba(0,0,0,0.12) 0 2px, transparent 2px 48px)` for plank seams plus a subtle `repeating-linear-gradient(0deg, rgba(255,255,255,0.03) 0 3px, transparent 3px 7px)` grain, chunky `border-4 border-[#3d2817]`, inner shadow for depth. Relative-positioned so children (clock, trays, document, stamps) can compose inside. Rounded none (pixel aesthetic).

**ShiftClock.tsx** —
```typescript
export type ShiftClockProps = { secondsLeft: number; totalSeconds: number };
```
Renders a small wall-clock card (`data-testid="sorter-shift-clock"`): label "SHIFT" (7px, white/60) over `M:SS` time (12px). Formatting: `${Math.floor(s/60)}:${String(s%60).padStart(2,'0')}`. Normal state: `#FFD93D` text on `#1a1a2e` bg with `border-4 border-black`. When `secondsLeft <= 10 && secondsLeft > 0`: text/border swap to `#FF6B9D` and the time element gets `animate-[clock-pulse_0.6s_ease-in-out_infinite]`. At 0 it renders `0:00` static (no pulse — shift's over, calm). Add `data-low={secondsLeft <= 10}` for test hooks. Purely presentational — parent owns the interval.

**OutgoingTray.tsx** —
```typescript
export type OutgoingTrayProps = { kind: 'keep' | 'redact'; count: number };
```
`data-testid={`sorter-tray-${kind}`}`. Renders a tray that fills visibly (SORTV2-13): a labeled base ("KEEP" in `#2ECC71` / "REDACT" in `#FF6B9D`, 10px) with up to `Math.min(count, 5)` stacked paper rectangles — small cream (`#F5EFD9`) divs with 1px dark borders, each offset 3px up and 1-2px alternating left/right from the last (stacked-paper illusion), rendered above the tray base. Below the stack: count line `×{count}` (9px, kind color) that bounces on increment — port the `prevCountRef` + `setBouncing` pattern from BucketZone.tsx verbatim (350ms, `animate-[counter-bounce_0.35s_ease-out]`), including the cleanup return. Empty tray (count 0) shows the base + "×0" dimmed (white/40).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function DeskSurface" client/src/components/phi-sorter/DeskSurface.tsx && grep -q "export function ShiftClock" client/src/components/phi-sorter/ShiftClock.tsx && grep -q "counter-bounce" client/src/components/phi-sorter/OutgoingTray.tsx</automated>
  </verify>
  <done>Three components compile with exact prop contracts; OutgoingTray bounce mirrors BucketZone's ref pattern; ShiftClock pulses only in the 1-10s window; no timers, no EventBridge imports.</done>
</task>

<task type="auto">
  <name>Task 2: StampPad + DeskDocument (the interaction surfaces)</name>
  <files>client/src/components/phi-sorter/StampPad.tsx, client/src/components/phi-sorter/DeskDocument.tsx</files>
  <action>
**StampPad.tsx** — renders BOTH stamps side by side (KEEP left, REDACT right — mirrors the retired NOT-PHI-left / PHI-right bucket layout so keyboard muscle memory carries over):
```typescript
export type StampKind = 'keep' | 'redact';
export type StampPadProps = {
  focusedStamp: StampKind | null;   // keyboard focus OR mouse hover — parent owns
  pressedStamp: StampKind | null;   // parent sets briefly on commit → drives stamp-press animation
  disabled: boolean;                // true during document transitions / non-sorting phases
  onStamp: (kind: StampKind) => void;
  onHover: (kind: StampKind | null) => void;
};
```
Each stamp is a `<button>` (`data-testid={`sorter-stamp-${kind}`}`) styled as a chunky rubber stamp: a wood-tone handle bar (`#8B6242`) over an ink base block — KEEP base `#1f4d33` with `#2ECC71` border + label, REDACT base `#4d1f2e` with `#FF6B9D` border + label (12px font). Sub-label under each (7px, white/50): "SAFE TO KEEP" / "THAT'S PHI". Key hints "←" / "→" (7px). Focused stamp gets `ring-4 ring-white/70 scale-105`; pressed stamp gets `animate-[stamp-press_0.15s_ease-out]`. `onMouseEnter`/`onMouseLeave` call `onHover`; `onClick` calls `onStamp(kind)` (button `disabled={disabled}` greys via opacity-50). NO drag handlers anywhere — click commits (SORTV2-12). Buttons set `tabIndex={-1}` (parent's window keydown handler owns keyboard; avoids double-commit on Enter via native button focus).

**DeskDocument.tsx** — the one-at-a-time paper chart:
```typescript
export type DocAnimState = 'entering' | 'active' | 'stamped' | 'exiting';
export type DeskDocumentProps = {
  item: SorterItemData;             // import type { SorterItem as SorterItemData } from '@/data/sorterData'
  animState: DocAnimState;
  stampedKind: StampKind | null;    // which ink mark shows during 'stamped'/'exiting'
  wasCorrect: boolean | null;       // drives flash-green / shake-red on the paper during 'stamped'
};
```
`data-testid={`sorter-desk-doc-${item.id}`}`. Paper aesthetic INVERTED from the old dark card (this is a physical document on a desk): cream `#F5EFD9` background, `border-4 border-[#b8a87e]`, chunky shadow `shadow-[6px_6px_0px_0px_rgba(0,0,0,0.45)]`, dark navy text. Header: "PATIENT CHART" rule line (8px, `#6B4A2F`) then `chart.patientName` (+ `, {age}` when present) at 11px `#1a1a2e`. Then the same conditional field rows as SorterItem.tsx (port ChartLine, restyled: label in `#8a6d3b`, value in `#2a2a3e`, 8px/1.6 line height): role → "Role", reasonForVisit → "Reason", emergencyContact → "Emergency Contact", doctorNote → "Doctor's Note", miscField → its own label. Max width ~420px, centered.

Animation classes by `animState` on the root: `entering` → `animate-[doc-slide-in_0.3s_ease-out]`; `active` → none; `stamped` → `wasCorrect === true` ? `animate-[flash-green_0.4s_ease-out]` : `wasCorrect === false` ? `animate-[shake-red_0.5s_ease-out]` : none; `exiting` → `stampedKind === 'keep'` ? `animate-[doc-slide-off-left_0.3s_ease-in_forwards]` : `animate-[doc-slide-off-right_0.3s_ease-in_forwards]` (forwards so it stays off-screen until unmount).

Ink mark (SORTV2-12 — renders when `stampedKind` is non-null, i.e. during 'stamped' and 'exiting'): absolutely positioned over the chart center-right, text "KEEP" (`#2ECC71`) or "REDACT" (`#FF6B9D`) at 16px inside a 4px double border of the same color, `animate-[ink-stamp-in_0.15s_ease-out_forwards]`, with `style={{ '--ink-rot': `${rot}deg` }}` where `rot` is deterministic per item: `((item.id.length * 7) % 13) - 6` (range −6..+6° — organic but stable across replays). `data-testid="sorter-ink-mark"`.

Ink-splatter burst (SORTV2-12): when `animState` transitions into 'stamped' (track via `prevAnimStateRef`, same transition-detection pattern as BucketZone's particle effect), spawn 8 absolutely-positioned 5px squares at the ink-mark location using the existing `sorter-particle` keyframe with `--dx`/`--dy` at 8 angles (port PARTICLE_ANGLES/PARTICLE_RADII from BucketZone, radii ~30/42px), colored by stamp ink (`#2ECC71` keep / `#FF6B9D` redact). Clear after 600ms with timeout cleanup. NOTE: ink color = stamp identity (Papers Please convention); correctness is still signaled separately by flash-green/shake-red + SFX + toast per SORTV2-12's decision.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export type StampKind" client/src/components/phi-sorter/StampPad.tsx && grep -q "doc-slide-off-left" client/src/components/phi-sorter/DeskDocument.tsx && grep -q "ink-stamp-in" client/src/components/phi-sorter/DeskDocument.tsx && grep -q "sorter-particle" client/src/components/phi-sorter/DeskDocument.tsx && ! grep -q "onDragStart\|draggable" client/src/components/phi-sorter/StampPad.tsx</automated>
  </verify>
  <done>StampPad: two click-commit stamps with focus/press/disabled states, zero drag code. DeskDocument: full Phase 22 chart rendering on paper styling, four-state animation contract, deterministic rotated ink mark, ink-colored splatter burst with cleanup. Both compile.</done>
</task>

<task type="auto">
  <name>Task 3: NPCReactionBubble persistent-portrait extension</name>
  <files>client/src/components/phi-sorter/NPCReactionBubble.tsx</files>
  <action>
Extend (do NOT rewrite) NPCReactionBubble for SORTV2-15 — the NPC must be visibly present above the desk for the whole encounter:

1. Add optional prop `spriteUrl?: string` to NPCReactionBubbleProps (JSDoc: "Phase 24 — resolved spritesheet path; renders a persistent 64px portrait. Sheets are 96×128, frame 0 top-left.").
2. When `spriteUrl` is provided: restructure the top-center layout into a row — portrait block on the left, speech bubble to its right. Portrait: 64×64 div using the Phase 21 CertificateOverlay pattern (`backgroundImage: url(spriteUrl)`, `backgroundSize: '192px 256px'`, `backgroundPosition: '0 0'`, `backgroundRepeat: 'no-repeat'`, `imageRendering: 'pixelated'`), on a `#1a1a2e` plate with `border-4 border-[#4FB3D9]`, name plate beneath it (npcName, 7px, `#FFD93D`) — `data-testid="sorter-npc-portrait"`. During a HOLD IT reveal the portrait border goes gold `border-[#FFD93D]` (matches the existing bubble treatment).
3. Persistence change: currently the component returns `null` when there's no text. When `spriteUrl` is present, the portrait + name plate ALWAYS render; only the bubble half hides when `visibleText` is empty. Keep the existing early-return behavior when `spriteUrl` is absent (backward compatibility — nothing else imports this component yet with the new prop, but don't break the Phase 22 contract).
4. Leave the existing fade-on-text-change, variant border colors, HOLD IT scale/gold-flash/educationalBeat logic untouched — reactions now visually originate beside the portrait, which is the whole point.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "spriteUrl" client/src/components/phi-sorter/NPCReactionBubble.tsx && grep -q "sorter-npc-portrait" client/src/components/phi-sorter/NPCReactionBubble.tsx && grep -q "pixelated" client/src/components/phi-sorter/NPCReactionBubble.tsx && npm run build</automated>
  </verify>
  <done>spriteUrl prop renders a persistent pixelated 64px portrait + name plate beside the bubble; without spriteUrl behavior is byte-for-byte equivalent to Phase 22/23; HOLD IT treatment intact; build passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run build` pass
- All five new components export exactly the specified prop types
- Zero imports of EventBridge, sorterReactions, or game-state hooks in the new components (presentational purity — `grep -L eventBridge` on all five)
- PHISorterOverlay.tsx untouched in this plan (`git diff --name-only` excludes it)
</verification>

<success_criteria>
Plan 03 can compose the desk format purely from these components without reading any other component source: stamps (click + focus visuals), one-doc paper chart with full animation lifecycle, filling trays, soft clock, persistent NPC portrait.
</success_criteria>

<output>
After completion, create `.planning/phases/24-phi-sorter-format-shift/24-02-SUMMARY.md`
</output>
