---
phase: 24-phi-sorter-format-shift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - client/src/phaser/scenes/BootScene.ts
  - client/src/data/sorterData.ts
  - client/src/index.css
autonomous: true
requirements: [SORTV2-11, SORTV2-12, SORTV2-13, SORTV2-14, SORTV2-15]

must_haves:
  truths:
    - "SORTV2-11..15 are defined in REQUIREMENTS.md with full text and traceability rows mapping each to Phase 24"
    - "BootScene preloads sfx_sorter_stamp and sfx_sorter_paper from already-vendored Kenney OGG files — no new binary assets added to the repo"
    - "Each of the 3 sorter document sets carries shiftSeconds (90 / 75 / 60) without any change to item category fields"
    - "index.css contains the 5 new desk-format keyframes (doc-slide-in, doc-slide-off-left, doc-slide-off-right, ink-stamp-in, stamp-press, clock-pulse)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SORTV2-11 through SORTV2-15 requirement definitions + traceability rows"
      contains: "SORTV2-15"
    - path: "client/src/phaser/scenes/BootScene.ts"
      provides: "Two new audio preload keys for stamp-thunk and paper-rustle"
      contains: "sfx_sorter_stamp"
    - path: "client/src/data/sorterData.ts"
      provides: "shiftSeconds field on SorterDocumentSet + per-set values"
      contains: "shiftSeconds"
    - path: "client/src/index.css"
      provides: "Phase 24 desk-format CSS keyframes"
      contains: "@keyframes doc-slide-in"
  key_links:
    - from: "client/src/phaser/scenes/BootScene.ts"
      to: "attached_assets/audio/kenney_impact-sounds/Audio/impactPlank_medium_000.ogg"
      via: "this.load.audio path"
      pattern: "impactPlank_medium_000"
    - from: "client/src/phaser/scenes/BootScene.ts"
      to: "attached_assets/audio/kenney_interface-sounds/Audio/scroll_002.ogg"
      via: "this.load.audio path"
      pattern: "scroll_002"
---

<objective>
Lay the Phase 24 foundation: define requirements SORTV2-11..15 (the format-shift spec), preload the two new SFX keys from already-shipped Kenney packs, add per-set shift durations to sorterData, and add the CSS keyframes the desk-format components (Plan 02) and overlay rewrite (Plan 03) will animate with.

Purpose: Phase 24 turns the PHI Sorter from a bucket-sort quiz into a Papers Please-style desk job. This plan is pure data/config/CSS groundwork — zero behavior change, build stays green, nothing user-visible yet.
Output: REQUIREMENTS.md with 5 new requirement IDs, 2 new BootScene audio keys, shiftSeconds in sorterData.ts, 6 new keyframes in index.css.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 24 section — success criteria 1-6 are the spec)
@.planning/REQUIREMENTS.md
@client/src/phaser/scenes/BootScene.ts (lines 88-105 — existing audio preload block)
@client/src/data/sorterData.ts (SorterDocumentSet type ~line 74; SET_1/SET_2/SET_3 definitions)
@client/src/index.css (lines 496-560 — existing sorter keyframes from Phases 16/23)
</context>

<audio_decision>
Phase 24 needs a stamp-thunk and a paper-rustle. NO new binary assets — both come from Kenney packs already vendored in attached_assets/audio/ (verified present on disk during planning):

| Key | File | Why |
|-----|------|-----|
| `sfx_sorter_stamp` | `kenney_impact-sounds/Audio/impactPlank_medium_000.ogg` | Wood-plank impact = closest available "rubber stamp on desk" thunk. Heavy enough to feel committed (Commandment 1). |
| `sfx_sorter_paper` | `kenney_interface-sounds/Audio/scroll_002.ogg` | Soft swish designed for sliding UI = closest available paper-slide rustle. |

Both packs are CC0 (License.txt vendored alongside). Precedent: Phase 16 sourced sfx_sorter_correct/wrong from kenney_interface-sounds the same way.
</audio_decision>

<tasks>

<task type="auto">
  <name>Task 1: Define SORTV2-11..15 in REQUIREMENTS.md and add foundation data (BootScene SFX + shiftSeconds)</name>
  <files>.planning/REQUIREMENTS.md, client/src/phaser/scenes/BootScene.ts, client/src/data/sorterData.ts</files>
  <action>
**REQUIREMENTS.md** — add a new subsection after the Phase 23 block (`### PHI Sorter Redesign — Feedback Moments (Phase 23)`), titled `### PHI Sorter Redesign — Format Shift (Phase 24)`, with these five unchecked requirements (follow the SORTV2-07..10 prose style — dense, testable, citing the mechanism):

- **SORTV2-11**: The sorter presents exactly one document at a time on a wood-desk surface — the multi-card scrollable pile is gone. Each document slides in from off-screen right (~300ms, slight settle wobble) accompanied by a paper-rustle SFX (`sfx_sorter_paper`, sourced from the already-vendored kenney_interface-sounds scroll_002.ogg — no new binary assets). Chart content rendering (patientName/role/reasonForVisit/emergencyContact/doctorNote/miscField from Phase 22) is preserved on a paper-styled document.
- **SORTV2-12**: KEEP and REDACT stamp buttons replace the PHI / NOT PHI buckets — clicking a stamp commits the call with no drag interaction anywhere in the encounter. Semantic mapping preserves HIPAA correctness: REDACT commits `category === 'phi'` (it IS PHI, must be redacted), KEEP commits `category === 'not_phi'` (safe to keep); item `category` fields in sorterData.ts are unchanged. Each stamp fires a stamp-thunk SFX (`sfx_sorter_stamp`, kenney impactPlank_medium_000.ogg), an ink mark (green KEEP / red REDACT, slight rotation) that persists on the document 250-400ms before it slides off, and an 8-particle ink-splatter burst in the stamp's ink color (reuses the Phase 23 `sorter-particle` keyframe). Correct/wrong distinction stays carried by the Phase 16/23 channels: green flash vs red shake on the document, sfx_sorter_correct/wrong, camera shake, and the educational toast.
- **SORTV2-13**: Stamped documents slide off the desk into one of two visible outgoing trays (KEEP left, REDACT right) rendered as stacked-paper visuals that fill as the shift progresses. Each tray shows a running count that bounces on increment (reuses Phase 23 `counter-bounce`); counts remain visible through end-of-shift and reset to 0 on encounter restart.
- **SORTV2-14**: A visible shift clock counts down per set — Set 1 = 90s, Set 2 = 75s, Set 3 = 60s (data-driven via `shiftSeconds` on SorterDocumentSet). Under 10s remaining the clock pulses for urgency. At 0:00 the encounter wraps softly with whatever has been stamped: unstamped items do not count toward score, no fail screen appears — the NPC delivers a "shift's over, leave the rest for the auditor" line and the normal completing → celebrating → debrief pipeline runs. scoreContribution remains `Math.round((correctCount / totalCount) * 12)` where totalCount is the FULL set size (unstamped items score as not-correct); SorterDebrief is unchanged.
- **SORTV2-15**: The trigger NPC's portrait (CSS-cropped spritesheet frame, Phase 21 CertificateOverlay pattern) + name stays persistently visible above the desk for the whole encounter; Phase 22-23 speech-bubble reactions (specific-item, band-fallback, band-transition, HOLD IT reveal) fire from the persistent portrait location. Keyboard parity holds: ←/→ focus the KEEP/REDACT stamp, Enter/Space commits the focused stamp, Esc aborts — keyboard-only completion fully supported. All Phase 22 (SORTV2-01..06) and Phase 23 (SORTV2-07..10) success criteria still hold under the new format.

Also append five rows to the Traceability table: `SORTV2-11..15 | Phase 24 | Pending`, and update the v2.1 coverage line (22 → 27 total).

**BootScene.ts** — after the existing sfx_sorter_wrong preload (line ~96), add:
```ts
this.load.audio('sfx_sorter_stamp', `${base}attached_assets/audio/kenney_impact-sounds/Audio/impactPlank_medium_000.ogg`);
this.load.audio('sfx_sorter_paper', `${base}attached_assets/audio/kenney_interface-sounds/Audio/scroll_002.ogg`);
```
Verify both files exist on disk first (`ls attached_assets/audio/kenney_impact-sounds/Audio/impactPlank_medium_000.ogg attached_assets/audio/kenney_interface-sounds/Audio/scroll_002.ogg`). Do NOT add or copy any audio files.

**sorterData.ts** — add `shiftSeconds: number;` to the `SorterDocumentSet` type (with a comment: `// Phase 24: per-set soft countdown — encounter wraps at 0:00, unstamped items don't score`), then add `shiftSeconds: 90` to SET_1, `shiftSeconds: 75` to SET_2, `shiftSeconds: 60` to SET_3. Touch NOTHING else in this file — no item, category, chart, or holdIt changes (HIPAA content is frozen).
  </action>
  <verify>
    <automated>grep -q "SORTV2-15" .planning/REQUIREMENTS.md && grep -q "sfx_sorter_stamp" client/src/phaser/scenes/BootScene.ts && grep -q "sfx_sorter_paper" client/src/phaser/scenes/BootScene.ts && grep -c "shiftSeconds" client/src/data/sorterData.ts | grep -q "4" && ls attached_assets/audio/kenney_impact-sounds/Audio/impactPlank_medium_000.ogg attached_assets/audio/kenney_interface-sounds/Audio/scroll_002.ogg && npx tsc --noEmit</automated>
  </verify>
  <done>SORTV2-11..15 defined with traceability rows; 2 new audio keys preload existing vendored files; all 3 sets carry shiftSeconds (90/75/60); zero category/chart changes in sorterData.ts (git diff shows only the type field + 3 value lines); typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 24 desk-format CSS keyframes to index.css</name>
  <files>client/src/index.css</files>
  <action>
Append a `/* ── Phase 24: PHI Sorter desk format (SORTV2-11..14) ── */` block after the existing Phase 23 sorter keyframes (after `completion-header-in`, ~line 555). Add exactly these keyframes — names are a CONTRACT consumed by Plan 02 components and Plan 03 overlay via Tailwind arbitrary `animate-[...]` values:

1. `@keyframes doc-slide-in` — document arrives from off-screen right with a settle wobble (Commandment 2: arrival has weight): `0% { transform: translateX(120%) rotate(2deg); opacity: 0.6; } 75% { transform: translateX(-2%) rotate(-0.5deg); opacity: 1; } 100% { transform: translateX(0) rotate(0); opacity: 1; }`
2. `@keyframes doc-slide-off-left` — stamped KEEP document exits toward the left tray: `0% { transform: translateX(0) rotate(0); opacity: 1; } 100% { transform: translateX(-140%) rotate(-6deg); opacity: 0.4; }`
3. `@keyframes doc-slide-off-right` — stamped REDACT document exits toward the right tray (mirror: `translateX(140%) rotate(6deg)`).
4. `@keyframes ink-stamp-in` — ink mark slams onto the paper: `0% { transform: scale(1.6) rotate(var(--ink-rot, -6deg)); opacity: 0; } 60% { transform: scale(0.95) rotate(var(--ink-rot, -6deg)); opacity: 1; } 100% { transform: scale(1) rotate(var(--ink-rot, -6deg)); opacity: 1; }` — uses CSS var `--ink-rot` so each stamp lands at a slightly different angle (organic feel).
5. `@keyframes stamp-press` — stamp button physically presses down on commit: `0% { transform: translateY(0) scale(1); } 40% { transform: translateY(5px) scale(0.94); } 100% { transform: translateY(0) scale(1); }`
6. `@keyframes clock-pulse` — low-time urgency pulse for the shift clock: `0%, 100% { transform: scale(1); } 50% { transform: scale(1.12); }` (color shift handled by a class swap, not the keyframe).

Reuse (do NOT duplicate): `sorter-particle` (ink splatter), `counter-bounce` (tray counts), `sorter-shake`, `sorter-score-pulse`, `completion-flash`, `completion-header-in`, `flash-green`, `shake-red` — all already exist from Phases 16/23.
  </action>
  <verify>
    <automated>grep -c "@keyframes doc-slide-in\|@keyframes doc-slide-off-left\|@keyframes doc-slide-off-right\|@keyframes ink-stamp-in\|@keyframes stamp-press\|@keyframes clock-pulse" client/src/index.css | grep -q "6" && npm run build</automated>
  </verify>
  <done>All 6 keyframes present with exact names; no existing keyframes duplicated or modified; production build passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run build` both pass
- `git diff client/src/data/sorterData.ts` shows ONLY the shiftSeconds type field + 3 value insertions — no category/chart/holdIt lines touched
- Both new BootScene audio paths resolve to files that exist on disk (no new binaries in the diff)
- REQUIREMENTS.md traceability table has 5 new Phase 24 rows
</verification>

<success_criteria>
SORTV2-11..15 fully defined and traceable; audio keys, shift durations, and animation keyframes in place as the contract surface for Plans 02 and 03; zero user-visible behavior change; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/24-phi-sorter-format-shift/24-01-SUMMARY.md`
</output>
