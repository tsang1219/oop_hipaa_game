---
phase: 23-phi-sorter-feedback-moments
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - client/src/components/phi-sorter/PHISorterOverlay.tsx
autonomous: true
requirements: [SORTV2-07, SORTV2-08, SORTV2-09, SORTV2-10]

must_haves:
  truths:
    - "Every drop (correct or wrong) shakes the sorter surface ~80ms at 2-3px and fires a color-matched particle burst on the destination bucket"
    - "Bucket counters increment live during the sort and read 0 at the start of every (re)played encounter"
    - "A visible SCORE readout pulses on each correct drop: +2 for the HOLD IT item, +1 for regular items; wrong drops add nothing"
    - "After the final drop: 600ms anticipation beat, then fanfare + completion overlay for ~1.2s (PERFECT n/n / GOOD / KEEP PRACTICING), then onComplete fires and SorterDebrief opens"
    - "NPC bubble tone shifts audibly with accuracy: crossing a band boundary surfaces a band line; repeated fallbacks cycle 3 variants per band"
    - "Phase 22 behaviors unregressed: HOLD IT reveal (sfx_fanfare 0.4 + gold bubble + 3.5s dwell), wrong-answer toast >=3s, keyboard parity, abort via Esc/X, scoreContribution = Math.round((correct/total) * 12)"
  artifacts:
    - path: "client/src/components/phi-sorter/PHISorterOverlay.tsx"
      provides: "Full Phase 23 feedback wiring: shake, counters, score pulse, celebrating phase, band-transition reactions"
      contains: "celebrating"
  key_links:
    - from: "client/src/components/phi-sorter/PHISorterOverlay.tsx"
      to: "client/src/components/phi-sorter/SorterCompletionOverlay.tsx"
      via: "rendered during phase === 'celebrating'"
      pattern: "SorterCompletionOverlay"
    - from: "client/src/components/phi-sorter/PHISorterOverlay.tsx"
      to: "client/src/components/phi-sorter/BucketZone.tsx"
      via: "count prop fed from per-bucket drop tallies"
      pattern: "count=\\{"
    - from: "client/src/components/phi-sorter/PHISorterOverlay.tsx"
      to: "client/src/index.css"
      via: "animate-[sorter-shake...] on the content wrapper, sorter-score-pulse on the score span"
      pattern: "sorter-shake"
    - from: "client/src/components/phi-sorter/PHISorterOverlay.tsx"
      to: "client/src/data/sorterReactions.ts"
      via: "getNPCFallbackReaction(npcId, band, nonce) with drop-count nonce"
      pattern: "getNPCFallbackReaction\\("
---

<objective>
Wire the Phase 23 feedback layer into PHISorterOverlay: per-drop camera shake + bucket counts, a pulsing local score (+2 HOLD IT / +1 regular), the 'celebrating' completion phase that shows SorterCompletionOverlay for ~1.2s before the debrief, and band-transition-aware NPC reaction selection — then verify zero Phase 22 regressions.

Purpose: This is where Commandments 1 and 8 land — every interaction now answers at proportional weight, and finishing the sort feels like a Zelda item-get rather than a form submission.
Output: Updated PHISorterOverlay.tsx; build + typecheck + regression greps clean.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 23 section)
@.planning/phases/23-phi-sorter-feedback-moments/23-01-SUMMARY.md
@client/src/components/phi-sorter/PHISorterOverlay.tsx
@client/src/components/phi-sorter/BucketZone.tsx (after Plan 01 — count prop, internal particles)
@client/src/components/phi-sorter/SorterCompletionOverlay.tsx (created by Plan 01)
@client/src/data/sorterReactions.ts (after Plan 01 — nonce-aware getNPCFallbackReaction)

Constraints:
- Do NOT modify UnifiedGamePage.tsx, SorterDebrief, sorterData.ts, or the EventBridge. The ~1.2s celebration lives entirely inside PHISorterOverlay by delaying the existing onComplete call — UnifiedGamePage's phase machine is untouched.
- CRITICAL prior bug (commit 90f41b3, noted in PHISorterOverlay comments): the completion timeout was once cleared by its own phase flip. Preserve the narrow-deps two-effect structure; the new 'celebrating' phase adds a THIRD narrow effect, it does not merge the existing two.
- scoreContribution formula `Math.round((correctCount / totalCount) * 12)` is sacred (SORTV2-05) — the new local sorter score is display-only and never flows into onComplete.
- Particles are already self-contained in BucketZone (feedbackState-driven, Plan 01); this plan only feeds `count` props and adds the surface shake.
</context>

<interfaces>
<!-- Contracts from Plan 01 — executor uses these directly. -->

From client/src/components/phi-sorter/SorterCompletionOverlay.tsx:
```typescript
export type SorterCompletionOverlayProps = { correctCount: number; totalCount: number };
export function SorterCompletionOverlay(props: SorterCompletionOverlayProps): JSX.Element;
// Presentational only — parent owns the ~1.2s timer, the fanfare SFX, and unmount.
```

From client/src/components/phi-sorter/BucketZone.tsx (Plan 01 addition):
```typescript
count?: number; // default 0 — running tally rendered as "N REDACTED" / "N KEPT", bounces on increment
```

From client/src/data/sorterReactions.ts (Plan 01 change):
```typescript
export function getNPCFallbackReaction(npcId: NPCSorterId, band: AccuracyBand, nonce?: number): NPCReaction;
// nonce cycles the 3 band lines deterministically: lines[nonce % 3]
export function accuracyToBand(correct: number, total: number): AccuracyBand;
```

CSS classes available (Plan 01, index.css): `animate-[sorter-shake_80ms_ease-in-out]`, `animate-[sorter-score-pulse_0.3s_ease-out]` (sorter-particle / counter-bounce / completion-* are consumed internally by the Plan 01 components).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire shake, counters, score pulse, celebrating phase, and band-transition reactions</name>
  <files>client/src/components/phi-sorter/PHISorterOverlay.tsx</files>
  <action>
All changes in PHISorterOverlay.tsx. Keep every existing Phase 16/22 behavior verbatim unless explicitly amended below.

**A. New state (follow the existing eslint-disable hook-comment style):**
- `bucketCounts: { phi: number; not_phi: number }` initial `{ phi: 0, not_phi: 0 }` — resets per mount, which covers SORTV2-08's restart criterion since UnifiedGamePage remounts the overlay per encounter.
- `sorterScore: number` initial 0.
- `scorePulseNonce: number` initial 0 — bumped on each score increment; used as React `key` to retrigger the pulse animation.
- `isShaking: boolean` initial false.
- `prevBandRef = useRef<AccuracyBand>('good')` — tracks the band as of the previous drop for transition detection.

**B. handleDrop additions (inside the existing useCallback; extend the dep array accordingly):**
1. Increment the destination bucket count on EVERY drop: `setBucketCounts(c => ({ ...c, [bucket]: c[bucket] + 1 }))`.
2. Camera shake on EVERY drop (SORTV2-07): `setIsShaking(true)` then `setTimeout(() => setIsShaking(false), 120)` (animation is 80ms; the 120ms clear gives it room without queuing). Drops can't occur faster than the animation, so a boolean is sufficient — no nonce needed.
3. Score increment ONLY on correct (SORTV2-10): `setSorterScore(s => s + (item.holdIt ? 2 : 1))` and `setScorePulseNonce(n => n + 1)`. Wrong drops: no score change, no pulse.
4. Band-transition reaction logic (SORTV2-10) — replace the current else-branch reaction lookup with:
   ```
   const newBand = accuracyToBand(newCorrectCount, newTotalDrops);
   const bandChanged = newBand !== prevBandRef.current && newTotalDrops >= 3; // ignore noisy first 2 drops
   prevBandRef.current = newBand;
   const specificReaction = getNPCReactionForItem(npcDisplay.id, itemId, isCorrect);
   const reaction = bandChanged
     ? getNPCFallbackReaction(npcDisplay.id, newBand, newTotalDrops)   // tone shift takes priority — the player HEARS the band change
     : specificReaction ?? getNPCFallbackReaction(npcDisplay.id, newBand, newTotalDrops);
   ```
   Keep the HOLD IT branch exactly as-is (it still takes precedence over everything, including band transitions) — but ALSO update `prevBandRef.current = newBand` inside the HOLD IT path so transition detection doesn't fire late on the next drop (compute newBand once, before the if/else).

**C. Completion sequence (SORTV2-09) — extend the phase machine:**
- `type SorterPhase = 'sorting' | 'completing' | 'celebrating'`.
- Effect 1 (pile empty → 'completing'): unchanged.
- Effect 2 (on 'completing'): currently waits 600ms → fanfare + onComplete. Change it to: wait `holdItReveal ? 2200 : 600` ms (if the FINAL drop was the HOLD IT item, let its reveal breathe before celebrating — read holdItReveal from state and include it in deps; it only changes via the final drop here, so no clear-own-timeout hazard), then `eventBridge.emit(REACT_PLAY_SFX, { key: 'sfx_fanfare', volume: 0.7 })` and `setPhase('celebrating')`. The fanfare now accompanies the celebration overlay's appearance instead of the debrief handoff — same single emission, moved one beat earlier.
- Effect 3 (NEW, on 'celebrating'): narrow effect mirroring Effect 2's structure and warning comment — `setTimeout(1200ms)` → call the existing `onComplete({...})` payload verbatim (encounterId, correctCount, totalCount, scoreContribution via the unchanged formula, takeaways). Deps: `[phase, correctCount, totalCount, encounterId, onComplete, docSet.takeaways]` — none of these change during 'celebrating', so the timeout survives. Copy the "do not merge these effects" comment pattern so future editors don't reintroduce the 90f41b3 bug.
- Render `{phase === 'celebrating' && <SorterCompletionOverlay correctCount={correctCount} totalCount={totalCount} />}` as the LAST child of the root overlay div (it's z-[60], pointer-events-none, so the close button beneath stays moot for 1.2s — acceptable).
- The keyboard effect already gates on `phase !== 'sorting'`, so input is dead during the celebration — correct, leave it.

**D. Render wiring:**
1. Camera shake: the root div is `absolute inset-0` and must NOT move (it owns the dark backdrop). Wrap the existing content (progress header + three-column row + keyboard hint) in ONE new inner div: `<div className={isShaking ? 'flex flex-col flex-1 animate-[sorter-shake_80ms_ease-in-out]' : 'flex flex-col flex-1'} data-testid="sorter-shake-surface">`. Move `flex flex-col` layout duties onto this wrapper as needed so the visual layout is pixel-identical when not shaking. NPCReactionBubble, close button, wrong-feedback toast, and SorterCompletionOverlay stay OUTSIDE the shake surface (bubble and toast are anchored overlays; shaking them looks like a bug, not juice).
2. Bucket counts: pass `count={bucketCounts.not_phi}` and `count={bucketCounts.phi}` to the respective BucketZone instances.
3. Score readout (SORTV2-10): in the progress header line, append ` · SCORE {sorterScore}` where the number lives in its own span: `<span key={scorePulseNonce} className="inline-block animate-[sorter-score-pulse_0.3s_ease-out] text-[#FFD93D]" data-testid="sorter-score">{sorterScore}</span>` — re-keying the leaf span restarts the animation on every increment without remounting anything else. (`inline-block` is required for transform-based scale to render.)
4. Update the component JSDoc header: add PHASE 23 bullet points (shake, counters, score pulse, celebrating phase, band-transition reactions).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npx tsc --noEmit && grep -q "'celebrating'" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "SorterCompletionOverlay" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "sorter-shake" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "sorter-score-pulse" client/src/components/phi-sorter/PHISorterOverlay.tsx</automated>
  </verify>
  <done>Every drop shakes the surface and increments its bucket counter; correct drops pulse the score (+2 HOLD IT / +1 regular); final drop → beat → fanfare + 1.2s SorterCompletionOverlay → onComplete; band crossings surface band lines with nonce cycling; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 22/16 regression verification pass</name>
  <files>client/src/components/phi-sorter/PHISorterOverlay.tsx</files>
  <action>
Run the full regression gate (ROADMAP criterion 6 / SORTV2-05). Fix anything that fails — expected state is all-pass with zero further edits.

1. `npm run build` — production build passes.
2. `npx tsc --noEmit` — clean.
3. Structural greps against PHISorterOverlay.tsx, all must hit:
   - `Math.round((correctCount / totalCount) * 12)` — score formula unchanged.
   - `sfx_fanfare', volume: 0.4` (HOLD IT reveal SFX) and `volume: 0.7` (completion fanfare) — both present, exactly one each.
   - `sfx_sorter_correct` and `sfx_sorter_wrong` — per-drop audio preserved.
   - `setTimeout(() => setWrongFeedback(null), 3500)` — wrong-answer toast dwell >=3s preserved.
   - `3500` HOLD IT dwell + `holdItReveal` still drives NPCReactionBubble's `holdIt` prop.
   - Keyboard handler still covers ArrowUp/ArrowDown/ArrowLeft/ArrowRight/Enter/' '/Escape and `onAbort` still wired to Esc + close button.
   - `takeaways: docSet.takeaways` in the onComplete payload.
4. Confirm UnifiedGamePage.tsx and SorterDebrief have zero diff this phase: `git diff --name-only main -- client/src/pages/UnifiedGamePage.tsx client/src/components/phi-sorter/SorterDebrief.tsx` is empty for Phase 23 commits (or `git log --oneline -3 -- <files>` shows no Phase 23 commits touching them).
5. Confirm BucketZone's Phase 16 feedback intact: `grep -q "flash-green_0.4s" client/src/components/phi-sorter/BucketZone.tsx && grep -q "shake-red_0.5s" client/src/components/phi-sorter/BucketZone.tsx`.

Document the grep results in the SUMMARY. Live playthrough verification (feel of shake amplitude, celebration pacing, NPC tone) is DEFERRED TO USER per the established Phase 16/17/22 pattern — note this explicitly in the SUMMARY with a 3-playthrough verification script (Set 1 Aiyana / Set 2 Marcus / Set 3 Dr. Tovar; check particles on both correct and wrong, counter bounce, score +2 on the HOLD IT item, PERFECT vs GOOD vs KEEP PRACTICING bands by deliberately varying accuracy, band-line tone shifts when dropping below 50% then climbing above 80%).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npm run build && grep -q "Math.round((correctCount / totalCount) \* 12)" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "volume: 0.4" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "sfx_sorter_wrong" client/src/components/phi-sorter/PHISorterOverlay.tsx && grep -q "flash-green_0.4s" client/src/components/phi-sorter/BucketZone.tsx</automated>
  </verify>
  <done>Build + typecheck pass; all Phase 16/22 structural invariants confirmed by grep; UnifiedGamePage/SorterDebrief untouched; SUMMARY records the deferred live-playthrough script for the user.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npx tsc --noEmit` both clean.
- Drop pipeline: counter increment + shake on every drop; score pulse only on correct; +2 only when `item.holdIt` is truthy.
- Completion pipeline: 'sorting' → 'completing' (600ms beat, or 2200ms if final drop was the HOLD IT reveal) → fanfare + 'celebrating' (1.2s SorterCompletionOverlay) → onComplete → SorterDebrief (unchanged UnifiedGamePage path).
- Reaction pipeline: HOLD IT > band-transition line > specific-item line > nonce-cycled band fallback.
- Phase 22 invariants verified by grep; counters/score reset on remount (replay after abort).
</verification>

<success_criteria>
- All six ROADMAP Phase 23 success criteria structurally satisfied: per-drop particles + shake (1), animated counters that reset (2), 1.2s band-colored completion overlay before debrief (3), pulsing score at +2/+1 weighting (4), enthusiasm-scaled NPC tone (5), zero Phase 22 regressions (6).
- Live feel calibration deferred to user with a written verification script in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/23-phi-sorter-feedback-moments/23-02-SUMMARY.md`
</output>
