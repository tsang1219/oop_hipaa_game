---
phase: 11-pre-restructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/saveData.ts
autonomous: true
requirements:
  - FOUN-03

must_haves:
  truths:
    - "migrateV1toV2(roomIds) reads all 29 v1 localStorage keys and writes a single pq:save:v2 JSON object"
    - "migrateV1toV2 is idempotent — if pq:save:v2 already exists it returns it without re-migrating"
    - "migrateV1toV2 cleans up all v1 keys (global + per-room) after writing v2"
    - "loadSave() returns the current v2 save or a clean default if absent"
    - "writeSave() atomically replaces pq:save:v2"
    - "sfxMuted and musicVolume are included in SaveDataV2 per user decision"
  artifacts:
    - path: "client/src/lib/saveData.ts"
      provides: "SaveDataV2 interface, migrateV1toV2, loadSave, writeSave exports"
      exports: ["SaveDataV2", "SAVE_KEY_V2", "defaultSave", "migrateV1toV2", "loadSave", "writeSave"]
  key_links:
    - from: "client/src/pages/PrivacyQuestPage.tsx"
      to: "client/src/lib/saveData.ts"
      via: "import { migrateV1toV2, loadSave, writeSave } from '@/lib/saveData'"
      pattern: "migrateV1toV2"
    - from: "client/src/pages/BreachDefensePage.tsx"
      to: "client/src/lib/saveData.ts"
      via: "import { loadSave, writeSave } from '@/lib/saveData'"
      pattern: "loadSave"
---

<objective>
Create `client/src/lib/saveData.ts` — the canonical save-data module for all v2 localStorage operations.

Purpose: Establish a single, versioned save schema before any restructure work begins. All downstream wiring (Plan 02) and future phases (12-15) read/write through this module rather than touching localStorage directly.
Output: New file `client/src/lib/saveData.ts` with TypeScript interface, migration function, and read/write helpers.
</objective>

<execution_context>
@/Users/all/.claude/get-shit-done/workflows/execute-plan.md
@/Users/all/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-pre-restructure-foundation/11-RESEARCH.md

**User decision:** sfxMuted and musicVolume MUST be included in SaveDataV2. BreachDefensePage will be updated in Plan 02 to read from the v2 object.
</context>

<interfaces>
<!-- v1 localStorage keys this migration reads (inventoried from PrivacyQuestPage.tsx lines 62-88): -->
<!-- Global: completedRooms, collectedStories, completedNPCs, completedZones, collectedEducationalItems -->
<!-- Global: current-privacy-score, final-privacy-score, gameStartTime, pq:onboarding:seen -->
<!-- Global: sfx_muted, music_volume -->
<!-- Per-room (× roomIds): resolvedGates_${roomId}, unlockedNpcs_${roomId}, pq:room:${roomId}:npcPulsed -->

<!-- Target key: pq:save:v2 -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create saveData.ts with SaveDataV2 schema and migration</name>
  <files>client/src/lib/saveData.ts</files>
  <behavior>
    - Test 1: migrateV1toV2([]) with no localStorage returns defaultSave (version=2, privacyScore=100, empty arrays)
    - Test 2: migrateV1toV2(['reception']) with v1 keys present reads them into v2 structure and removes v1 keys
    - Test 3: migrateV1toV2 called twice with same v1 data returns v2 on second call without re-migrating
    - Test 4: migrateV1toV2 with corrupted pq:save:v2 (invalid JSON) falls through and migrates fresh from v1
    - Test 5: loadSave() returns defaultSave when pq:save:v2 absent; returns parsed object when present
    - Test 6: writeSave(data) stores JSON at pq:save:v2; loadSave() round-trips it correctly
    - Test 7: per-room keys (resolvedGates_reception, unlockedNpcs_reception, pq:room:reception:npcPulsed) are migrated and removed
    - Test 8: sfxMuted maps from sfx_muted='true' → true; musicVolume maps from music_volume='0.8' → 0.8
  </behavior>
  <action>
Create `client/src/lib/saveData.ts` with the exact schema and logic from the research. Key implementation points:

1. Export the `SaveDataV2` interface with these exact fields:
   ```typescript
   interface SaveDataV2 {
     version: 2;
     completedRooms: string[];
     collectedStories: string[];
     completedNPCs: string[];
     completedZones: string[];
     collectedItems: string[];
     privacyScore: number;
     finalPrivacyScore: number;
     resolvedGates: Record<string, string[]>;
     unlockedNpcs: Record<string, string[]>;
     npcPulsedRooms: string[];
     gameStartTime: number;
     onboardingSeen: boolean;
     sfxMuted: boolean;
     musicVolume: number;
   }
   ```

2. Export `SAVE_KEY_V2 = 'pq:save:v2'`

3. Export `defaultSave: SaveDataV2` with sensible defaults (privacyScore=100, musicVolume=0.6, gameStartTime set at module load)

4. Export `migrateV1toV2(roomIds: string[]): SaveDataV2`:
   - Check if `pq:save:v2` exists — if valid JSON, return it immediately (idempotent)
   - If corrupted JSON, fall through to migrate
   - Build v2 from v1: read each global key with individual try/catch per key
   - Read per-room keys for each roomId: `resolvedGates_${roomId}`, `unlockedNpcs_${roomId}`, `pq:room:${roomId}:npcPulsed`
   - sfx_muted → sfxMuted (string 'true' → boolean), music_volume → musicVolume (parseFloat)
   - pq:onboarding:seen === '1' → onboardingSeen: true
   - collectedEducationalItems → collectedItems (key rename in v1 was 'collectedEducationalItems')
   - Write v2 to localStorage FIRST, then delete v1 keys (write-before-delete prevents data loss on crash)
   - V1 global keys to delete: completedRooms, collectedStories, completedNPCs, completedZones, collectedEducationalItems, current-privacy-score, final-privacy-score, gameStartTime, pq:onboarding:seen, sfx_muted, music_volume
   - Per-room keys to delete (for each roomId): resolvedGates_${roomId}, unlockedNpcs_${roomId}, pq:room:${roomId}:npcPulsed
   - Return the written v2 save

5. Export `loadSave(): SaveDataV2`:
   - Read pq:save:v2 — if absent return `{ ...defaultSave }`
   - try JSON.parse; on error return `{ ...defaultSave }`

6. Export `writeSave(data: SaveDataV2): void`:
   - localStorage.setItem(SAVE_KEY_V2, JSON.stringify(data))

Create the test file at `client/src/lib/saveData.test.ts` first (RED phase), implement until tests pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/all/claude_code/oop_hipaa_games/PrivacyQuest && npx vitest run client/src/lib/saveData.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 behavior tests pass. saveData.ts exports SaveDataV2, SAVE_KEY_V2, defaultSave, migrateV1toV2, loadSave, writeSave. TypeScript compiles with no errors (npx tsc --noEmit).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports no TypeScript errors in saveData.ts
- `npx vitest run client/src/lib/saveData.test.ts` — all tests pass
- saveData.ts exports all 6 symbols: SaveDataV2 (type), SAVE_KEY_V2, defaultSave, migrateV1toV2, loadSave, writeSave
- sfxMuted and musicVolume are in the SaveDataV2 interface
</verification>

<success_criteria>
- `client/src/lib/saveData.ts` exists and compiles
- All 8 TDD tests pass
- migrateV1toV2 is idempotent (safe to call on every boot)
- Per-room key migration handles roomIds parameter correctly
- No v1 key survives migration (all 29 cleared)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pre-restructure-foundation/11-01-SUMMARY.md`
</output>
